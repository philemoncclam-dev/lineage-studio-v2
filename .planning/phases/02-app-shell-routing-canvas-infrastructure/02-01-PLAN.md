---
phase: 02-app-shell-routing-canvas-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/vitest.config.ts
  - frontend/src/test/setup.ts
  - frontend/.gitignore
autonomous: false
requirements: [SHELL-07]
user_setup: []

must_haves:
  truths:
    - "`npm run build` (tsc -b && vite build) still passes after the install — no version conflict breaks the existing toolchain"
    - "`npx vitest run` executes and reports 0 tests found (or a trivial smoke test) rather than erroring — the frontend now has a working test runner where none existed"
    - "Build order is load-bearing: this plan leaves the app runnable (`npm run build` green) and demoable before any shell/router work begins; no intermediate commit leaves it broken (SHELL-07 ordering edge)"
    - "`routeTree.gen.ts` is git-ignored so the Vite-plugin-generated route tree (introduced in 02-03) is never committed"
    - "All packages installed match the exact versions verified in 02-RESEARCH.md Package Legitimacy Audit; no [SLOP]-verdict package is introduced"
  artifacts:
    - path: "frontend/vitest.config.ts"
      provides: "Vitest jsdom test environment + setup wiring reusing the existing @vitejs/plugin-react"
      contains: "jsdom"
    - path: "frontend/src/test/setup.ts"
      provides: "@testing-library/jest-dom matcher registration for component tests"
    - path: "frontend/package.json"
      provides: "test/test:run npm scripts + the Phase-2 runtime and dev dependencies"
      contains: "vitest"
  key_links:
    - from: "frontend/vitest.config.ts"
      to: "frontend/src/test/setup.ts"
      via: "test.setupFiles references the setup module"
      pattern: "setupFiles"
---

<objective>
Install the Phase-2 dependency set (TanStack Router + Zod + cmdk + the Radix
primitives) and stand up the frontend's first test runner (Vitest +
Testing Library + jsdom), which does not exist today. This is the Wave-0
toolchain foundation every later plan's `<automated>` verification depends on.

Purpose: 02-03 through 02-06 all consume these packages and assert behavior
through Vitest. Nothing can be built or verified until they exist. Per the
package legitimacy protocol, every install is gated behind a blocking human
verification because 02-RESEARCH.md flagged the TanStack/Radix/Vitest packages
`[SUS]` (publish-recency heuristic) and every package name is `[ASSUMED]`.

Output: A green `npm run build`, a working `npx vitest run`, the full
dependency set from 02-RESEARCH.md installed at the audited versions, and
`routeTree.gen.ts` git-ignored ahead of 02-03's router codegen.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-app-shell-routing-canvas-infrastructure/02-RESEARCH.md
@frontend/package.json
@frontend/vite.config.ts
@frontend/tsconfig.app.json
</context>

## Artifacts this phase produces (this plan)

- New file `frontend/vitest.config.ts` — Vitest config (jsdom environment, globals, `setupFiles`, reuses `@vitejs/plugin-react`).
- New file `frontend/src/test/setup.ts` — imports `@testing-library/jest-dom`.
- `frontend/package.json` new npm scripts: `test` (watch) and `test:run` (single-run), plus new `dependencies` (`@tanstack/react-router`, `zod`, `cmdk`, `@radix-ui/react-dialog`, `@radix-ui/react-dropdown-menu`, `@radix-ui/react-tooltip`, `@radix-ui/react-visually-hidden`) and `devDependencies` (`@tanstack/router-plugin`, `@tanstack/react-router-devtools`, `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `jsdom`).
- `frontend/.gitignore` new entry `routeTree.gen.ts`.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification (blocking, security gate)</name>
  <files>frontend/package.json</files>
  <action>Pause before any `npm install` and hold execution until a human confirms the legitimacy of the `[SUS]`/`[ASSUMED]` packages from 02-RESEARCH.md's Package Legitimacy Audit (T-02-SC supply-chain gate). Present the verification steps below; do not install any package until the human types "approved". This checkpoint is never auto-approvable.</action>
  <what-built>Nothing yet — this gate runs BEFORE any `npm install`. It exists because 02-RESEARCH.md's Package Legitimacy Audit gave a `[SUS]` (`too-new` publish-date heuristic) verdict to `@tanstack/react-router`, `@tanstack/router-plugin`, every `@radix-ui/*` package, `vitest`, and `@testing-library/jest-dom`, and tagged every package name `[ASSUMED]` for provenance. Threat T-02-SC (supply chain) requires a human confirmation that these are the established packages, not typosquats.</what-built>
  <how-to-verify>
    1. Open each package's registry page and confirm it is the real, long-lived project with tens-of-millions weekly downloads and a matching source repo, exactly as the audit table records:
       - https://npmjs.com/package/@tanstack/react-router  (repo github.com/TanStack/router)
       - https://npmjs.com/package/@tanstack/router-plugin  (repo github.com/TanStack/router)
       - https://npmjs.com/package/zod  (repo github.com/colinhacks/zod)
       - https://npmjs.com/package/cmdk  (repo github.com/pacocoursey/cmdk)
       - https://npmjs.com/package/@radix-ui/react-dialog  (repo github.com/radix-ui/primitives) — the other `@radix-ui/*` packages ship from the same monorepo/timestamp
       - https://npmjs.com/package/vitest  (repo github.com/vitest-dev/vitest)
       - https://npmjs.com/package/@testing-library/jest-dom  (repo github.com/testing-library/jest-dom)
    2. Confirm the package NAMES match exactly (no added/altered scope, no character swaps).
    3. This is a fast confirmation, not a deep investigation — the audit already recorded the download counts and repos; you are confirming they are not typosquats.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with installs, or name any package that does not check out.</resume-signal>
  <verify>
    <human-check>Human has visited each package's npmjs.com page, confirmed the source repo + download counts match the audit, confirmed the package names are exact (no typosquat), and typed "approved".</human-check>
  </verify>
  <done>Human has confirmed all `[SUS]`/`[ASSUMED]` packages are legitimate and approved installation; execution may proceed to Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Install dependencies + stand up Vitest/jsdom + git-ignore the generated route tree</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/vitest.config.ts, frontend/src/test/setup.ts, frontend/.gitignore</files>
  <read_first>
    - frontend/package.json — current dependency/script set (React 19.2.7, Vite 8.1.1, @vitejs/plugin-react 6.0.3, @tailwindcss/vite 4.3.3)
    - frontend/vite.config.ts — existing plugin composition (react + tailwindcss) that the Vitest config reuses
    - frontend/tsconfig.app.json — bundler moduleResolution, verbatimModuleSyntax, strict flags the test files must satisfy
    - .planning/phases/02-app-shell-routing-canvas-infrastructure/02-RESEARCH.md — "Standard Stack" (exact versions) + "Validation Architecture" > Wave 0 Gaps
  </read_first>
  <action>
    From `frontend/`, install the exact versions in 02-RESEARCH.md's Standard Stack. Runtime deps: `@tanstack/react-router@1.170.18`, `zod@4.4.3`, `cmdk@1.1.1`, `@radix-ui/react-dialog@1.1.20`, `@radix-ui/react-dropdown-menu@2.1.21`, `@radix-ui/react-tooltip@1.2.13`, `@radix-ui/react-visually-hidden@1.2.8`. Dev deps: `@tanstack/router-plugin@1.168.23`, `@tanstack/react-router-devtools@1.167.0`, `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `jsdom`. Do NOT install `@tanstack/zod-adapter` (Zod v4 Standard Schema makes it unnecessary — 02-RESEARCH.md Alternatives Considered) and do NOT install `@radix-ui/react-popover`/`@radix-ui/react-tabs` (supporting-tier, unused this phase).
    Create `frontend/vitest.config.ts` as a `defineConfig` from `vitest/config` that reuses the React plugin, sets `test.environment` to `jsdom`, `test.globals` true, and `test.setupFiles` pointing at `src/test/setup.ts`. Keep it a standalone file so `vite.config.ts` is untouched (02-03 owns the router-plugin edit to `vite.config.ts`).
    Create `frontend/src/test/setup.ts` importing `@testing-library/jest-dom` so DOM matchers register for component tests.
    Add npm scripts `test` (vitest watch) and `test:run` (`vitest run`) to `package.json`.
    Append `routeTree.gen.ts` to `frontend/.gitignore` (02-03's `@tanstack/router-plugin` regenerates it every build; it is never hand-edited or committed — 02-RESEARCH.md Runtime State Inventory).
    Do not add a placeholder test that would fail — an empty run reporting "no tests" is the acceptance target.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npx vitest run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` `dependencies` contains `@tanstack/react-router`, `zod`, `cmdk`, `@radix-ui/react-dialog`, `@radix-ui/react-dropdown-menu`, `@radix-ui/react-tooltip`, `@radix-ui/react-visually-hidden`
    - `frontend/package.json` `devDependencies` contains `@tanstack/router-plugin`, `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `jsdom`
    - `frontend/package.json` `scripts` contains a `test:run` entry invoking `vitest run`
    - `cd frontend && npm run build` exits 0 (existing tsc + vite build unbroken by the new deps)
    - `cd frontend && npx vitest run` exits 0 (runner works; "no test files" is an acceptable pass)
    - `frontend/vitest.config.ts` references `setupFiles` and `jsdom`
    - `frontend/.gitignore` contains a line `routeTree.gen.ts`
    - `frontend/package.json` does NOT contain `@tanstack/zod-adapter`
  </acceptance_criteria>
  <done>Phase-2 dependencies installed at audited versions; `npm run build` and `npx vitest run` both green; generated route tree git-ignored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → developer machine | Untrusted third-party package code executes at install/build/test time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | npm installs (TanStack / Radix / cmdk / Vitest) | high | mitigate | Blocking human-verify checkpoint (Task 1) confirms each `[SUS]`/`[ASSUMED]` package against its npmjs.com page + source repo before install; versions pinned to the 02-RESEARCH.md audit; no `[SLOP]` package installed. Never auto-approvable. |
</threat_model>

<verification>
- `cd frontend && npm run build` exits 0.
- `cd frontend && npx vitest run` exits 0.
- `git check-ignore frontend/routeTree.gen.ts` (or grepping `.gitignore`) confirms the entry.
</verification>

<success_criteria>
Dependencies installed at audited versions behind the blocking legitimacy gate; a working Vitest/jsdom runner exists; the build stays green; `routeTree.gen.ts` is git-ignored.
</success_criteria>

<output>
Create `.planning/phases/02-app-shell-routing-canvas-infrastructure/02-01-SUMMARY.md` when done.
</output>

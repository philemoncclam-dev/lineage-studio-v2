---
phase: 02-app-shell-routing-canvas-infrastructure
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified:
  - frontend/src/shell/AppShell.tsx
  - frontend/src/shell/ModeMenu.tsx
  - frontend/src/shell/Rail.tsx
  - frontend/src/shell/RailBottomCluster.tsx
  - frontend/src/shell/Inspector.tsx
  - frontend/src/shell/CommandPalette.tsx
  - frontend/src/shell/railConfig.ts
  - frontend/src/shell/theme.ts
  - frontend/src/shell/__tests__/Rail.test.tsx
  - frontend/src/styles/components.css
  - frontend/src/styles/shell.css
  - frontend/src/routes/purview/push.tsx
  - frontend/src/routes/purview/data-products.tsx
  - frontend/src/views/LineageView.tsx
  - frontend/src/views/GraphView.tsx
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-04, SHELL-07]

must_haves:
  truths:
    - "A persistent left icon rail lists the current mode's top-level destinations; it fills full viewport height and never scrolls with canvas content (SHELL-01)"
    - "The rail renders one item per entry in the mode's destination config array (railConfig) — adding a fifth destination is a one-line config edit requiring no structural/JSX change (SHELL-01)"
    - "Two rail config entries never merge — N config entries produce exactly N rail buttons, rendered in the declared array order, stable across renders (SHELL-01 adjacency/ordering edges)"
    - "A mode with a single destination renders one rail item; the rail-bottom cluster (Cmd+K trigger, theme toggle, connection-status dot) is present in every mode identically (SHELL-01 empty edge / D-05)"
    - "The canvas region fills 100% of the remaining viewport width/height and chrome recedes visually — the rail is ~48px icon-only + tooltips (SHELL-02 / D-04)"
    - "Bridged LineageView/GraphView size to their new container via flex/grid (not fixed-viewport assumptions) — no double-scrollbar and no cut-off canvas edge against the rail/inspector (SHELL-02 overflow edge / Pitfall 3 / D-15)"
    - "At no commit do the bridged canvases and new shell chrome present two different visual languages — the bridged canvases read Phase-1 tokens only, no raw hex reintroduced (SHELL-07 adjacency edge / Pitfall 14)"
    - "The liked top-bar button + segmented-control visual treatment is carried forward: new shell controls reuse the existing .seg/.tbtn/.search component classes/tokens rather than inventing parallel class names (SHELL-04 / D-16)"
    - "Mode switching happens via an app-logo dropdown menu (Radix DropdownMenu) — no segmented control or rail icon switches modes (D-02); the current mode is checkmarked"
    - "Rail items are icon-only with a Radix Tooltip label AND a persistent VisuallyHidden accessible name; the accessible name matches the UI-SPEC Copywriting Contract exactly (e.g. 'Push to Purview', 'Toggle theme', 'Search (⌘K)')"
    - "D-13: The inspector width is fixed via the --inspector-width tier-3 token (384px, within the 360–400px band); the shell defines it once here, with no content-driven resizing (revisited only if Phase 3 evidence needs more room)"
    - "The theme toggle sets data-theme on <html> and persists the choice to localStorage (key lineage-studio-theme); removing it falls back to OS prefers-color-scheme — the canvasTokens MutationObserver picks up the change with no manual invalidate call"
    - "The Purview Push and Data Products placeholder pages show the exact UI-SPEC empty-state heading (text-display 22px) and body copy, with no CTA — honest placeholders until Phase 5"
    - "Rail item interaction states follow the tier-3 tokens: default --rail-item-fg → hover --rail-item-fg-hover/--rail-item-bg-hover → active (current destination) --rail-item-fg-active/--rail-item-bg-active; accent marks only the current destination, never a hover preview"
    - "Rail tooltips and node/table names do not clip destructively — tooltips size to content; long names use the existing ellipsis + nowrap pattern"
    - "Every new shell component (rail, mode menu, rail-bottom cluster, placeholder pages) is checked in BOTH light and dark theme before this plan is done (standing discipline #12)"
  prohibitions:
    - statement: "MUST NOT present the Purview Push or Data Products pages as if they were functional — no confirm/action button that silently no-ops; each placeholder page must plainly state it is not built yet (transparency)"
      status: unverified
    - statement: "MUST NOT introduce any live Purview/Fabric write path in this phase — Push and Data Products stay read-only placeholders; the real write path is Phase 5 (safety; CLAUDE.md dry-run-only invariant)"
      status: unverified
  artifacts:
    - path: "frontend/src/shell/Rail.tsx"
      provides: "Generic icon rail rendering from a mode's railConfig array (Tooltip + VisuallyHidden per item)"
    - path: "frontend/src/shell/railConfig.ts"
      provides: "Per-mode destination arrays (graph/lineage/purview) — the single source SHELL-01's 'add a 5th, no structural change' depends on"
    - path: "frontend/src/shell/ModeMenu.tsx"
      provides: "App-logo Radix DropdownMenu mode switcher (Graph / Lineage / Purview), current mode checkmarked (D-02)"
    - path: "frontend/src/shell/RailBottomCluster.tsx"
      provides: "Cmd+K search trigger, theme toggle, connection-status dot — identical in every mode (D-05)"
    - path: "frontend/src/shell/theme.ts"
      provides: "setTheme(light|dark|null) with localStorage persistence + OS fallback"
      exports: ["setTheme"]
    - path: "frontend/src/shell/Inspector.tsx"
      provides: "Non-modal overlay Inspector STUB mounted by AppShell (renders null until sel) — filled by 02-05"
    - path: "frontend/src/shell/CommandPalette.tsx"
      provides: "cmdk Command.Dialog STUB mounted by AppShell with open state — filled by 02-06"
    - path: "frontend/src/styles/components.css"
      provides: "New tier-3 shell tokens (--rail-*, --mode-menu-*, --inspector-*, --palette-*, --rail-bottom-*, --theme-toggle-*, --status-dot-*) resolving to Phase-1 tier-2 semantics"
  key_links:
    - from: "frontend/src/shell/Rail.tsx"
      to: "frontend/src/shell/railConfig.ts"
      via: "Rail maps over the current mode's config array (data-driven, not hardcoded JSX)"
      pattern: "railConfig|RAIL"
    - from: "frontend/src/shell/AppShell.tsx"
      to: "frontend/src/shell/Inspector.tsx"
      via: "AppShell mounts <Inspector/> and <CommandPalette/> as the fixed overlay mount points 02-05/02-06 fill"
      pattern: "Inspector|CommandPalette"
    - from: "frontend/src/shell/RailBottomCluster.tsx"
      to: "frontend/src/shell/theme.ts"
      via: "theme toggle button calls setTheme; canvasTokens observer reacts"
      pattern: "setTheme"
---

<objective>
Build the mode-based shell chrome onto the router root from 02-03: the
app-logo mode switcher (Radix DropdownMenu, D-02), the per-mode icon-only
left rail rendered from a config array (D-01/D-03/D-04), the rail-bottom
cluster (Cmd+K trigger, theme toggle, connection status, D-05), the new
tier-3 component tokens, honest Purview placeholder pages, and the
container-fit bridge that makes the existing LineageView/GraphView fill the
new canvas region without visual regression (D-15/Pitfall 3). Also mount
(as stubs) the Inspector and CommandPalette overlays 02-05/02-06 will fill.

Purpose: This delivers the persistent rail (SHELL-01), the receding chrome +
full-viewport canvas (SHELL-02), and the carried-forward liked segmented/
button treatment (SHELL-04) — while keeping the app demoable (SHELL-07). The
rail is data-driven so a fifth destination is a one-line change.

Output: `src/shell/{AppShell,ModeMenu,Rail,RailBottomCluster,theme,railConfig}`
+ Inspector/CommandPalette stubs, new tier-3 tokens in `components.css` + a
`shell.css`, filled Purview placeholder pages, and bridged canvases.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-app-shell-routing-canvas-infrastructure/02-CONTEXT.md
@.planning/phases/02-app-shell-routing-canvas-infrastructure/02-UI-SPEC.md
@.planning/phases/02-app-shell-routing-canvas-infrastructure/02-PATTERNS.md
@frontend/src/styles/components.css
@frontend/src/styles/tokens.css
@frontend/src/views/GraphView.tsx
@frontend/src/views/LineageView.tsx
</context>

## Artifacts this phase produces (this plan)

New files: `src/shell/AppShell.tsx` (fills the 02-03 minimal root: logo/ModeMenu + Rail + RailBottomCluster + canvas Outlet region + Inspector/CommandPalette mounts), `src/shell/ModeMenu.tsx`, `src/shell/Rail.tsx`, `src/shell/RailBottomCluster.tsx`, `src/shell/railConfig.ts` (`railConfig` per-mode arrays), `src/shell/theme.ts` (`setTheme`), `src/shell/Inspector.tsx` (stub), `src/shell/CommandPalette.tsx` (stub), `src/shell/__tests__/Rail.test.tsx`, `src/styles/shell.css`. New tier-3 tokens appended to `src/styles/components.css`: `--rail-width`, `--rail-bg`, `--rail-border`, `--rail-item-size`, `--rail-item-icon-size`, `--rail-item-radius`, `--rail-item-fg`, `--rail-item-fg-hover`, `--rail-item-bg-hover`, `--rail-item-fg-active`, `--rail-item-bg-active`, `--mode-menu-*`, `--inspector-*`, `--palette-*`, `--rail-bottom-gap`, `--theme-toggle-size`, `--status-dot-size`, `--status-dot-ok`, `--status-dot-off`, `--status-dot-err`. Modified: Purview placeholder routes, LineageView/GraphView (container-fit only).

<tasks>

<task type="auto">
  <name>Task 1: Tier-3 shell tokens + mode menu + data-driven rail + rail-bottom cluster + AppShell composition</name>
  <files>frontend/src/shell/AppShell.tsx, frontend/src/shell/ModeMenu.tsx, frontend/src/shell/Rail.tsx, frontend/src/shell/RailBottomCluster.tsx, frontend/src/shell/railConfig.ts, frontend/src/shell/theme.ts, frontend/src/shell/Inspector.tsx, frontend/src/shell/CommandPalette.tsx, frontend/src/shell/__tests__/Rail.test.tsx, frontend/src/styles/components.css, frontend/src/styles/shell.css</files>
  <read_first>
    - .planning/phases/02-app-shell-routing-canvas-infrastructure/02-UI-SPEC.md — "New Tier-3 Component Tokens" (exact token names/values), "Layout & Composition" (48px rail, overlay stacking z-index table), "Copywriting Contract" accessible-names table (exact aria-label text per control), "Color" (accent only on current destination)
    - frontend/src/styles/tokens.css — the REAL Phase-1 token names the new tier-3 tokens must resolve to: spacing is `--spacing-1..16` (NOT `--space-*` as the UI-SPEC shorthands them), plus `--color-surface-1/2/3`, `--color-accent`, `--color-accent-dim`, `--color-success`, `--color-destructive`, `--radius-control`, `--radius-card`, `--shadow-overlay`, `--text-display`
    - frontend/src/styles/components.css — existing tier-3 block (append into it, do not create a second :root block) + the `.seg`/`.tbtn`/`.search`/`.src-chip` treatment to carry forward (SHELL-04/D-16)
    - .planning/phases/02-app-shell-routing-canvas-infrastructure/02-CONTEXT.md — D-02 (logo mode menu), D-03 (per-mode rail contents), D-04 (icon-only ~48px), D-05 (rail-bottom cluster contents)
    - .planning/phases/02-app-shell-routing-canvas-infrastructure/02-RESEARCH.md — theme-toggle Code Example (THEME_KEY localStorage + attribute), "Don't Hand-Roll" (Tooltip + VisuallyHidden for icon buttons)
    - frontend/src/routes/__root.tsx — the minimal AppShell shell this plan fleshes out; the RouterContext graph + ModelProvider already wired
  </read_first>
  <action>
    Append the UI-SPEC "New Tier-3 Component Tokens" block into the EXISTING tier-3 `:root` block in `components.css`, each resolving to a Phase-1 tier-2 semantic. Use the real token names from `tokens.css`: e.g. `--rail-width: var(--spacing-12)` (48px), `--rail-item-fg: var(--color-text-tertiary)`, `--rail-item-fg-active: var(--color-accent)`, `--rail-item-bg-active: var(--color-accent-dim)`, `--mode-menu-bg: var(--color-surface-3)`, `--inspector-width: 384px`, `--inspector-bg: var(--color-surface-2)`, `--palette-bg: var(--color-surface-3)`, `--status-dot-ok: var(--color-success)`, `--status-dot-err: var(--color-destructive)`. Introduce NO new hex value and NO new primitive.
    Build `src/shell/railConfig.ts` as per-mode arrays of `{ key, label, icon, to }` destination items (graph: drill scope/filters/layout; lineage: dataset scope/filters/trace tools; purview: Push/Definitions/Data Products) — Claude has latitude on icons per D-03, but the accessible-name text is LOCKED to the UI-SPEC Copywriting Contract table. Build `src/shell/Rail.tsx` to map over the current mode's config array (data-driven, so a fifth entry needs only a config line), each item an icon-only button wrapped in a Radix Tooltip with a VisuallyHidden accessible name; active item (current route) gets the accent treatment, hover uses the hover tokens (never accent). Build `src/shell/ModeMenu.tsx` as a Radix DropdownMenu triggered by the app logo, listing the three modes with the current one checkmarked, navigating on select (D-02). Build `src/shell/RailBottomCluster.tsx` with the Cmd+K search trigger (opens the palette, reuses `.search` treatment), the theme toggle, and a tri-state connection-status dot (`--status-dot-ok/off/err`) driven by the existing `fetchPurviewStatus` one-shot check (preserve one-shot, do not add polling — 02-RESEARCH.md Open Question 1). Build `src/shell/theme.ts` `setTheme(theme)` writing `data-theme` on `document.documentElement` + persisting to localStorage key `lineage-studio-theme`, removing the attribute+key to fall back to OS preference; do not call `invalidateCanvasTokens` (the bootstrap observer handles it).
    Compose all of the above into `src/shell/AppShell.tsx`: logo+ModeMenu region, Rail, canvas region wrapping `<Outlet/>`, RailBottomCluster, and the overlay mounts `<Inspector/>` + `<CommandPalette open onOpenChange/>`. Create `src/shell/Inspector.tsx` as a STUB returning null (02-05 fills it) and `src/shell/CommandPalette.tsx` as a STUB rendering an empty `Command.Dialog` gated on open state (02-06 fills it) — AppShell owns the palette open-state + the global Cmd+K keydown listener (ported from App.tsx lines 58-64). Put layout/structural CSS in `src/styles/shell.css` (import it from AppShell or main); keep all colour/spacing via tokens, geometry literals allowed per components.css convention. Apply the overlay z-index stacking from the UI-SPEC table. Write `src/shell/__tests__/Rail.test.tsx` asserting the rail renders exactly N buttons for an N-entry config and that each carries its accessible name (SHELL-01).
    Do NOT invent parallel class names for the segmented/button treatment — reuse `.seg`/`.tbtn`/`.search` (SHELL-04/D-16). Do NOT switch modes via a segmented control or rail icon (D-02).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/shell/__tests__/Rail.test.tsx --reporter=dot && npm run build</automated>
    <human-check>Run `npm run dev`; confirm in BOTH themes (toggle mid-check): (1) the left icon rail shows per-mode items with tooltips + hover/active states, accent only on the current destination; (2) clicking the app logo opens the mode switcher and switching modes changes the rail; (3) the theme toggle flips the app and survives a reload; (4) the connection-status dot reflects backend reachability.</human-check>
  </verify>
  <acceptance_criteria>
    - `src/shell/Rail.tsx` renders from `railConfig` (maps an array; no per-destination hardcoded JSX)
    - `src/shell/__tests__/Rail.test.tsx` passes and asserts button count equals config length + presence of accessible names
    - `components.css` contains `--rail-width`, `--inspector-width`, `--status-dot-ok`, and each new tier-3 token resolves to a `var(--color-*/--spacing-*/--radius-*/--shadow-*)` (no raw hex added)
    - `src/shell/theme.ts` exports `setTheme` and references `localStorage` + `data-theme`
    - `src/shell/ModeMenu.tsx` uses a Radix DropdownMenu (not a `.seg` segmented control)
    - `src/shell/AppShell.tsx` mounts both `<Inspector` and `<CommandPalette`
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>Mode-based shell chrome (logo menu + data-driven rail + rail-bottom cluster + theme toggle) composed on the router root with new tier-3 tokens; Inspector/CommandPalette mount points in place; Rail unit test green; verified in both themes.</done>
</task>

<task type="auto">
  <name>Task 2: Container-fit bridge for LineageView/GraphView + honest Purview placeholder pages</name>
  <files>frontend/src/views/LineageView.tsx, frontend/src/views/GraphView.tsx, frontend/src/routes/purview/push.tsx, frontend/src/routes/purview/data-products.tsx, frontend/src/styles/shell.css</files>
  <read_first>
    - frontend/src/views/LineageView.tsx — its `.ls-body`/`.ls-stage`/`.ls-canvas` sizing; confirm it assumes it owns the viewport under the old top bar (the assumption Pitfall 3 breaks)
    - frontend/src/views/GraphView.tsx — `.gv-root`/`.gv-stage`/`.gv-canvas-wrap` sizing + the resize()/clientWidth reads that must still get a correctly-sized container
    - frontend/src/styles/components.css — `.app`/`.ls-body`/`.gv-root` flex rules the new shell layout replaces the frame around
    - .planning/phases/02-app-shell-routing-canvas-infrastructure/02-UI-SPEC.md — "Copywriting Contract" (exact Push/Data Products heading + body strings, text-display 22px) and the "overflow (container)" + "empty" UI Considerations rows
    - .planning/phases/02-app-shell-routing-canvas-infrastructure/02-CONTEXT.md — D-15 (token bridge only — no throwaway styling Phases 3-4 delete), D-03 (Purview honest placeholders)
  </read_first>
  <action>
    Make `LineageView` and `GraphView` size to whatever container the new shell canvas region gives them — switch any fixed-viewport assumption (e.g. relying on the old `.app` column height under the removed top bar) to flex/grid fill so there is no double-scrollbar and no cut-off edge against the rail/inspector (Pitfall 3/D-15). This is container-fit ONLY: do NOT touch their internal rendering logic, hover/trace, force sim, or column edges — Phases 3-4 rebuild those. Do NOT reintroduce raw hex/px colour; the bridged canvases already read Phase-1 tokens and must keep reading only those (SHELL-07 adjacency / Pitfall 14). Confirm `GraphView`'s canvas `resize()` still receives a correctly-sized wrapper after the reframe.
    Fill `routes/purview/push.tsx` and `routes/purview/data-products.tsx` as honest placeholder pages using the UI-SPEC Copywriting Contract verbatim: Push heading "Push to Purview isn't built yet" (text-display, 22px/600) + its exact body sentence; Data Products heading "Data product cataloguing isn't built yet" + its exact body sentence. No CTA, no action button, no confirm control — the page must plainly read as not-yet-built (transparency prohibition). Style via tokens + shell.css; text-display is consumed here for the first time (a genuine page-level title with no card context).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <human-check>Run `npm run dev`; confirm (1) both canvases fill the canvas region with no double-scrollbar and no edge cut off against the rail; (2) the Purview Push and Data Products rail destinations show the exact placeholder headings/body with no action button; (3) canvases and shell read as ONE visual language (no stray old hex) in both themes.</human-check>
  </verify>
  <acceptance_criteria>
    - `routes/purview/push.tsx` contains the string "Push to Purview isn't built yet"
    - `routes/purview/data-products.tsx` contains the string "Data product cataloguing isn't built yet"
    - Neither placeholder page renders a `<button>` action/confirm CTA
    - `LineageView`/`GraphView` diffs are container-sizing only (no changes to trace/force/edge logic) and introduce no raw hex colour literal
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>Old canvases fill the new canvas region cleanly in both themes; Purview Push/Data Products are honest, CTA-free placeholders with the locked copy.</done>
</task>

</tasks>

## Flagged edge-probe assumptions (surfaced, not dropped)

The spec-less edge probe returned `unclassified` (`unresolved`) rows for **SHELL-02** ("canvas fills the remaining viewport; chrome recedes") and **SHELL-04** ("carried-forward top-bar treatment"). Surfaced as explicit planner assumptions rather than dropped: both are covered by the truths above (canvas flex-fill / no double-scrollbar for SHELL-02; reuse of `.seg`/`.tbtn`/`.search` for SHELL-04). The un-enumerated edge is a subjective "chrome recedes"/"treatment matches" visual judgment — verified by the both-themes human-check, which 02-UI-SPEC and standing discipline #12 own.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App state → localStorage (theme) | The only new persisted state; contains a theme string, no secret or PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | theme localStorage value | low | accept | A tampered `lineage-studio-theme` value only sets the `data-theme` attribute; `setTheme` writes only `light`/`dark`/removes the key — an unexpected value falls back to OS preference, no privileged effect |
| T-02-02b | Information Disclosure | Purview placeholder pages | low | accept | Placeholder pages render only static copy; no live Purview data, no write path, nothing to disclose (reinforced by the no-live-write prohibition) |
</threat_model>

<verification>
- `npx vitest run src/shell/__tests__/Rail.test.tsx` green.
- `npm run build` green.
- Manual (both themes): rail per-mode items + tooltips + active/hover states; mode menu switches modes; theme toggle persists; canvases fill container with no double-scrollbar; placeholder pages show locked copy, no CTA.
</verification>

<success_criteria>
Persistent data-driven icon rail (SHELL-01), full-viewport receding-chrome canvas (SHELL-02), carried-forward liked segmented/button treatment (SHELL-04), app demoable with one coherent visual language (SHELL-07); honest Purview placeholders; Inspector/CommandPalette mount points ready for Wave 4.
</success_criteria>

<output>
Create `.planning/phases/02-app-shell-routing-canvas-infrastructure/02-04-SUMMARY.md` when done.
</output>

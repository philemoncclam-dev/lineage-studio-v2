---
phase: 02-app-shell-routing-canvas-infrastructure
plan: 05
type: execute
wave: 4
depends_on: [02-03, 02-04]
files_modified:
  - frontend/src/shell/Inspector.tsx
  - frontend/src/shell/__tests__/Inspector.test.tsx
  - frontend/src/views/LineageView.tsx
  - frontend/src/views/GraphView.tsx
  - frontend/src/styles/shell.css
autonomous: true
requirements: [SHELL-03]

must_haves:
  truths:
    - "A contextual right-hand inspector opens whenever a node/column is selected (?sel is set) and closes when it is cleared — driven entirely by useSelection, mirroring the ?sel URL param (SHELL-03 / D-11)"
    - "The inspector is a non-modal overlay floating over the canvas region's right edge with elevation — it never participates in canvas layout flow, so opening/closing it causes ZERO reflow of the canvas beneath it (SHELL-03 / D-10)"
    - "The inspector shows a real metadata card derived from the loaded LineageGraph/AppModel: name, kind, workspace/lakehouse location, column list for tables, and connected-edge counts (N in / N out) (D-12)"
    - "Esc, the close button, and an empty-canvas click all resolve to the same useSelection().clear() call — one selection-clearing path, not duplicated per-canvas handlers (D-11)"
    - "A missing metadata field omits its row entirely rather than rendering a blank value; a table with zero columns omits the column section rather than showing an empty list (UI-SPEC 'partial' consideration)"
    - "The inspector body scrolls internally (overflow-y auto) below a fixed header and never grows past the viewport height (UI-SPEC 'overflow' consideration)"
    - "The inspector title and any long node/column name use the existing ellipsis + nowrap pattern so a long name cannot break the panel layout (UI-SPEC 'long-text' consideration)"
    - "Canvas selection writes go only through useSelection().select() (replace:true) — a node/column click sets ?sel/?col without pushing a history entry (SHELL-06 contract preserved)"
    - "Inspector open/close is instant this phase (no transition) — an honest baseline; the transition-not-snap treatment is Phase 7 (MOT-02), not a regression here"
    - "The inspector is checked in BOTH light and dark theme before this plan is done (standing discipline #12)"
  artifacts:
    - path: "frontend/src/shell/Inspector.tsx"
      provides: "Full non-modal overlay Inspector — reads useSelection + useModel, renders the D-12 metadata card, clear() on close"
      exports: ["Inspector"]
    - path: "frontend/src/shell/__tests__/Inspector.test.tsx"
      provides: "Renders-on-sel / null-when-clear + partial-field omission unit coverage"
  key_links:
    - from: "frontend/src/shell/Inspector.tsx"
      to: "frontend/src/selection/useSelection.ts"
      via: "renders iff useSelection().sel is set; close button calls clear()"
      pattern: "useSelection"
    - from: "frontend/src/views/GraphView.tsx"
      to: "frontend/src/selection/useSelection.ts"
      via: "node click calls select(nodeId) (replace:true), feeding the inspector via ?sel"
      pattern: "select\\("
---

<objective>
Fill the Inspector overlay stub from 02-04 into the real non-modal contextual
inspector (D-10/D-12) and wire canvas selection into it through
`useSelection`. Clicking a node/column on the bridged canvases sets `?sel`/
`?col`; the inspector floats over the canvas's right edge showing a metadata
card derived from the loaded graph; Esc / close button / empty-canvas click
all clear the selection.

Purpose: Delivers SHELL-03 (contextual inspector opens on selection, closes
without disturbing canvas layout) and makes the shared selection plumbing from
02-03 observable end-to-end. D-10's non-modal overlay is the specific reason
this is a plain floating `<aside>`, not a Radix Dialog/Popover (which would
trap focus / block the canvas).

Output: Full `src/shell/Inspector.tsx` + test, selection click wiring in the
bridged `LineageView`/`GraphView`, inspector overlay CSS in `shell.css`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-app-shell-routing-canvas-infrastructure/02-CONTEXT.md
@.planning/phases/02-app-shell-routing-canvas-infrastructure/02-RESEARCH.md
@.planning/phases/02-app-shell-routing-canvas-infrastructure/02-PATTERNS.md
@.planning/phases/02-app-shell-routing-canvas-infrastructure/02-UI-SPEC.md
@frontend/src/views/LineageView.tsx
@frontend/src/views/GraphView.tsx
</context>

## Artifacts this phase produces (this plan)

Full `src/shell/Inspector.tsx` (`Inspector` component — non-modal overlay, D-12 metadata card, clear-on-close), `src/shell/__tests__/Inspector.test.tsx`, inspector overlay CSS in `src/styles/shell.css`, and selection click wiring added to `src/views/LineageView.tsx` + `src/views/GraphView.tsx` (calls into `useSelection().select()`).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the non-modal overlay Inspector with the D-12 metadata card</name>
  <files>frontend/src/shell/Inspector.tsx, frontend/src/shell/__tests__/Inspector.test.tsx, frontend/src/styles/shell.css</files>
  <read_first>
    - .planning/phases/02-app-shell-routing-canvas-infrastructure/02-RESEARCH.md — Pattern 3 (non-modal overlay inspector: conditional `<aside>`, single shell-level Esc listener, NOT a Dialog/Popover) + the Anti-Patterns note on wrapping the inspector in Dialog/Popover
    - .planning/phases/02-app-shell-routing-canvas-infrastructure/02-PATTERNS.md — Inspector section: the `.ls-inspector` structural analog (LineageView lines 140-161) to copy STRUCTURALLY but NOT its `flex: none` docked layout; reposition as position:absolute/fixed off the canvas right edge, fixed width per D-13
    - .planning/phases/02-app-shell-routing-canvas-infrastructure/02-UI-SPEC.md — inspector tokens (`--inspector-width: 384px`, `--inspector-bg: var(--color-surface-2)`, `--inspector-shadow`), z-index 20 in the overlay stacking table, the "populated"/"partial"/"overflow"/"long-text" UI Considerations rows, and the "Close inspector" accessible name
    - .planning/phases/02-app-shell-routing-canvas-infrastructure/02-CONTEXT.md — D-10 (overlay, never reflows), D-11 (Esc/close/empty-click clears, visibility == selection), D-12 (metadata card fields), D-13 (fixed ~360-400px width)
    - frontend/src/model.tsx (or src/model/index.tsx) — AppModel shape: tables (columns, layer, name), context (up/down edge counts), levels — the data the card derives from
    - frontend/src/selection/useSelection.ts — the sel/col/clear API from 02-03
  </read_first>
  <behavior>
    - Inspector returns null when useSelection().sel is unset; renders the card when sel is set
    - For a selected table node: shows name, kind, workspace/lakehouse location, its column list, and connected-edge counts (N in / N out) derived from the model
    - A missing field omits its row; a table with zero columns omits the column section (no empty list)
    - The close control calls useSelection().clear()
  </behavior>
  <action>
    Build `src/shell/Inspector.tsx` exporting `Inspector`: read `useSelection()` and `useModel()`; return null unless `sel` is set (visibility == selection, D-11). Render a floating `<aside role="complementary" aria-label>` positioned absolutely/fixed off the canvas region's right edge with elevation (`--inspector-bg` surface-2, `--inspector-shadow`, `--inspector-width` 384px) so it overlays the canvas and never reflows it (D-10) — do NOT use `flex: none`/docked layout and do NOT wrap it in a Radix Dialog/Popover (Pattern 3 anti-pattern). Resolve the selected node from `?sel` (and `?col` when present) against `useModel()` and render the D-12 metadata card: name (`text-heading`), kind (`text-micro` mono), workspace/lakehouse location, column list for tables (reuse the existing `.col` row treatment), and connected-edge counts as `Connections (N in / N out)` using the `.sec-t` label pattern. Omit any missing field's row and omit the column section entirely for a zero-column table (partial consideration). Give the body `overflow-y: auto` below a fixed header so it never exceeds viewport height (overflow consideration); ellipsize the title/long names (long-text consideration). Close button carries the accessible name "Close inspector" and calls `clear()`. Put the overlay CSS in `shell.css` using the inspector tokens. Instant show/hide (no transition) is the correct baseline this phase.
    Write `src/shell/__tests__/Inspector.test.tsx`: renders null when sel unset; renders the metadata card for a selected sample table (name + a column + edge counts present); a table with missing/zero fields omits the corresponding rows/section.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/shell/__tests__/Inspector.test.tsx --reporter=dot && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/shell/Inspector.tsx` exports `Inspector`; returns null when `sel` is unset
    - The inspector `<aside>` is positioned as an overlay (position absolute/fixed), NOT a flex-docked `flex: none` aside, and is not wrapped in a Radix `Dialog`/`Popover`
    - Close control has the accessible name "Close inspector" and calls `clear()`
    - `src/shell/__tests__/Inspector.test.tsx` passes and includes both a renders-card case and a partial-field-omission case
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>Inspector is a non-modal right-edge overlay showing the D-12 metadata card, omitting missing fields, scrolling internally, closing via clear(); unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Wire canvas selection + single Esc/empty-click clear into the inspector</name>
  <files>frontend/src/views/LineageView.tsx, frontend/src/views/GraphView.tsx, frontend/src/styles/shell.css</files>
  <read_first>
    - frontend/src/views/LineageView.tsx — its internal `selected`/`hover` column state + the `.ls-inspector` render it currently hosts (lines 133, 138-179): the column click handlers become selection writes and its own docked inspector is superseded by the shell overlay
    - frontend/src/views/GraphView.tsx — its node `pick`/`onClick` handler (lines 156-168) where a node selection write hooks in, and its `Escape` keydown (lines 36-39) that must not double-handle clearing
    - frontend/src/selection/useSelection.ts — select(nodeId, colKey) (replace:true) + clear()
    - .planning/phases/02-app-shell-routing-canvas-infrastructure/02-RESEARCH.md — Pattern 3 (single shell-level Esc listener — do NOT duplicate per-canvas Esc-to-clear) + Pitfall 1 (all selection writes via useSelection, never a raw navigate)
    - .planning/phases/02-app-shell-routing-canvas-infrastructure/02-CONTEXT.md — D-11 (Esc/close/empty-canvas click all clear), D-15 (bridge wiring, not throwaway styling)
  </read_first>
  <action>
    Wire the bridged canvases to emit selection through `useSelection().select()` (never a raw `navigate({ search })` — Pitfall 1, replace:true is centralized): in `GraphView`, a node click at the table/detail level sets `?sel` (and drives the shell inspector); in `LineageView`, a column click sets `?sel`/`?col`. Replace `LineageView`'s own docked `.ls-inspector` render with the shell overlay Inspector as bridge wiring (D-15 permits selection wiring; this removes a redundant second inspector rather than adding throwaway styling) — the column-detail data now surfaces through the shell metadata card, and Phase 3 deepens it with transforms/evidence. Route Esc-to-clear and empty-canvas-click-to-clear through the single shell-level path (`clear()` from `useSelection`) — do not add a second per-canvas Esc handler that clears selection (the GraphView Escape that pops a drill level is a different concern and stays). Add any small overlay-fit CSS to `shell.css`. Keep changes to selection/inspector wiring only — do not touch force sim, column-edge geometry, or hover-trace logic (Phases 3-4).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <human-check>Run `npm run dev`; confirm in BOTH themes: (1) clicking a graph node / a lineage column opens the right-edge inspector with that item's metadata and updates ?sel/?col in the URL; (2) the canvas does NOT shift/reflow when the inspector opens or closes; (3) Esc, the close button, and an empty-canvas click each close the inspector; (4) clicking through several nodes then pressing browser Back returns to the pre-selection state (selection did not flood history).</human-check>
  </verify>
  <acceptance_criteria>
    - `GraphView.tsx` and `LineageView.tsx` call `useSelection().select(` for selection (no raw `navigate({ search` for selection writes)
    - `LineageView.tsx` no longer renders its own `.ls-inspector` overlay (superseded by the shell Inspector)
    - Only one Esc-to-clear-selection path exists (shell-level); the GraphView drill-pop Escape is unchanged
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>Canvas selection flows through useSelection into the shell inspector; open/close never reflows the canvas; Esc/close/empty-click all clear; back-button not flooded by selection.</done>
</task>

</tasks>

## Flagged edge-probe assumptions (surfaced, not dropped)

The spec-less edge probe returned an `unclassified` (`unresolved`) row for **SHELL-03** ("inspector opens on selection and closes without disturbing canvas layout"). Surfaced as an explicit planner assumption rather than dropped: SHELL-03 is covered by the truths above (overlay never reflows the canvas / opens-on-sel / closes-on-clear). The un-enumerated edge is the "without disturbing canvas layout" no-reflow guarantee, which is a visual/layout judgment verified by the both-themes human-check (open/close causes zero canvas shift).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Inspector renders node/column names + counts already present in the in-memory loaded graph; no new input or network surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Information Disclosure | Inspector metadata card | low | accept | Renders only graph data already loaded and already shown on the canvas; no new data is exposed. Node/column names are rendered as JSX text children (React auto-escapes), never dangerouslySetInnerHTML. |
</threat_model>

<verification>
- `npx vitest run src/shell/__tests__/Inspector.test.tsx` green.
- `npm run build` green.
- Manual (both themes): selection opens the overlay with correct metadata; open/close causes zero canvas reflow; Esc/close/empty-click clear; back-button not flooded.
</verification>

<success_criteria>
Contextual non-modal inspector opens on selection and closes without disturbing canvas layout (SHELL-03), driven by the shared ?sel/?col plumbing, with a real graph-derived metadata card; verified in both themes.
</success_criteria>

<output>
Create `.planning/phases/02-app-shell-routing-canvas-infrastructure/02-05-SUMMARY.md` when done.
</output>

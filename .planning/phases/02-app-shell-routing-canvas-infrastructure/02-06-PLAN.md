---
phase: 02-app-shell-routing-canvas-infrastructure
plan: 06
type: execute
wave: 4
depends_on: [02-03, 02-04]
files_modified:
  - frontend/src/shell/CommandPalette.tsx
  - frontend/src/shell/search.ts
  - frontend/src/shell/__tests__/search.test.ts
  - frontend/src/styles/shell.css
  - frontend/src/views/SearchPalette.tsx
  - frontend/src/views/search.css
autonomous: true
requirements: [NAV-01, NAV-03]

must_haves:
  truths:
    - "Cmd+K (and the rail-bottom search trigger) opens a command palette searching tables, columns, notebooks, and grep-style matches over notebook code (NAV-01)"
    - "The palette is built on cmdk's Command.Dialog (Radix Dialog underneath) so it is fully keyboard-operable — arrow navigation, Enter to select, Esc to close, correct focus-trap and focus-restore-on-close come from the primitive, not hand-rolled handlers (NAV-03)"
    - "The existing ranking/grouping is preserved: GROUP_ORDER (table→column→notebook→code), GROUP_LABEL, and MAX_PER_GROUP=8 are ported unchanged, and Command is given shouldFilter={false} so cmdk never re-sorts or re-filters the app's own ranked/capped results (Pattern 4 / Pitfall 6 / research assumption A2 resolved: keep the app's exact-substring ranking, do not adopt cmdk fuzzy)"
    - "Selecting a result performs a real navigation via useSelection/navigate — a table/column result navigates to the lineage route with ?sel/?col set (not a selection-only update), so the palette result lands the user on the canvas with the item selected (NAV-01)"
    - "With no query the palette renders nothing below the input (query.trim() guard carried forward); with a query and no matches it shows the exact copy 'No matches for \"{query}\".' (UI-SPEC empty consideration)"
    - "Result groups render in the fixed GROUP_ORDER with per-kind group labels and the per-group cap (UI-SPEC zero-one-many consideration)"
    - "Long identifiers / code lines in palette rows use the existing ellipsis + nowrap treatment so a long match cannot break the row layout (UI-SPEC long-text consideration)"
    - "The old hand-rolled SearchPalette.tsx (manual ArrowUp/ArrowDown/Enter/Escape + document keydown focus handling) is retired — it is deleted, not left dead in the tree (D-17)"
    - "The palette is checked in BOTH light and dark theme before this plan is done (standing discipline #12)"
  artifacts:
    - path: "frontend/src/shell/CommandPalette.tsx"
      provides: "cmdk Command.Dialog palette — shouldFilter=false, ported grouped/capped results, real navigation on select"
      exports: ["CommandPalette"]
    - path: "frontend/src/shell/search.ts"
      provides: "Ported search()/GROUP_ORDER/GROUP_LABEL/MAX_PER_GROUP/hl + SearchResult type (moved out of SearchPalette.tsx)"
      exports: ["search", "SearchResult"]
    - path: "frontend/src/shell/__tests__/search.test.ts"
      provides: "Ranking/grouping/cap parity unit tests for the ported search() logic (NAV-01)"
  key_links:
    - from: "frontend/src/shell/CommandPalette.tsx"
      to: "frontend/src/shell/search.ts"
      via: "feeds Command.List from search() output with shouldFilter={false}"
      pattern: "shouldFilter"
    - from: "frontend/src/shell/CommandPalette.tsx"
      to: "frontend/src/selection/useSelection.ts"
      via: "onSelect navigates to the lineage route with ?sel/?col (real navigation)"
      pattern: "navigate|useSelection"
---

<objective>
Rebuild the Cmd+K command palette on `cmdk`'s `Command.Dialog` (D-17),
retiring the hand-rolled `SearchPalette.tsx`. The palette searches tables,
columns, notebooks, and notebook code (NAV-01) and is fully keyboard-operable
via the primitive rather than manual key handlers (NAV-03). The existing
grouped/capped/ranked `search()` logic is ported verbatim and fed to `cmdk`
with `shouldFilter={false}` so the app keeps its own ranking; selecting a
result performs a real navigation that lands the user on the canvas with the
item selected.

Purpose: Delivers NAV-01 and NAV-03 on the new token/component primitives,
and retires one of the codebase's hand-rolled widgets (custom keyboard nav +
`document.addEventListener` focus handling) in favor of the maintained,
accessible Radix-Dialog-backed primitive — that retirement is itself part of
this phase's intent.

Output: Full `src/shell/CommandPalette.tsx` (filling the 02-04 stub) +
`src/shell/search.ts` (ported logic) + ranking parity test, palette CSS in
`shell.css`, and deletion of `src/views/SearchPalette.tsx` / `search.css`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-app-shell-routing-canvas-infrastructure/02-CONTEXT.md
@.planning/phases/02-app-shell-routing-canvas-infrastructure/02-RESEARCH.md
@.planning/phases/02-app-shell-routing-canvas-infrastructure/02-PATTERNS.md
@.planning/phases/02-app-shell-routing-canvas-infrastructure/02-UI-SPEC.md
@frontend/src/views/SearchPalette.tsx
</context>

## Artifacts this phase produces (this plan)

Full `src/shell/CommandPalette.tsx` (`CommandPalette` — cmdk Command.Dialog), `src/shell/search.ts` (ported `search`, `GROUP_ORDER`, `GROUP_LABEL`, `MAX_PER_GROUP`, `hl`, `SearchResult` type, `notebookIndex`), `src/shell/__tests__/search.test.ts`, palette CSS moved into `src/styles/shell.css`. Deleted: `src/views/SearchPalette.tsx`, `src/views/search.css`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port search()/ranking into src/shell/search.ts with parity tests</name>
  <files>frontend/src/shell/search.ts, frontend/src/shell/__tests__/search.test.ts</files>
  <read_first>
    - frontend/src/views/SearchPalette.tsx — the full port source: `SearchResult` type (lines 7-15), `GROUP_ORDER`/`GROUP_LABEL`/`MAX_PER_GROUP` (lines 23-27), `notebookIndex` (lines 29-42), `search()` (lines 44-81), `hl()` highlight helper (lines 84-100), and the grouped/capped assembly in the `results` memo (lines 109-117)
    - .planning/phases/02-app-shell-routing-canvas-infrastructure/02-PATTERNS.md — CommandPalette section: "Ranking/grouping logic to preserve verbatim" + the note that this feeds cmdk with shouldFilter={false}
    - frontend/src/model.tsx (or src/model/index.tsx) — AppModel (tables/columns/notebooks/levels/notebookCode) the search reads
  </read_first>
  <behavior>
    - search(model, '') returns [] (empty query guard)
    - search(model, q) returns table/column/notebook/code matches; grouped assembly emits groups in GROUP_ORDER, each capped at MAX_PER_GROUP=8
    - a code match carries the notebook label, a line number, and the matched line text as context
    - hl(text, q) splits text into plain segments + <mark> segments for each case-insensitive occurrence (no HTML string concatenation)
  </behavior>
  <action>
    Move the `SearchResult` type, `GROUP_ORDER`, `GROUP_LABEL`, `MAX_PER_GROUP`, `notebookIndex`, `search()`, and `hl()` verbatim from `SearchPalette.tsx` into `src/shell/search.ts` (export `search`, `SearchResult`, and the ranking constants). Do not alter the ranking, grouping, or cap semantics — this is a lift, not a rewrite (Pitfall 6: the app keeps its own ranking). Write `src/shell/__tests__/search.test.ts` asserting: empty-query returns []; a query matching multiple kinds returns them in GROUP_ORDER; a group exceeding 8 matches is capped at 8; a code match carries label + line + context. Keep `hl()` returning React nodes (plain-text segments + `<mark>` elements), never an HTML string.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/shell/__tests__/search.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `src/shell/search.ts` exports `search` and the `SearchResult` type
    - `MAX_PER_GROUP` is 8 and `GROUP_ORDER` is table→column→notebook→code
    - `src/shell/__tests__/search.test.ts` passes and includes an empty-query-returns-empty case and a per-group-cap case
    - `hl()` produces React nodes / `<mark>` elements, not an HTML string
  </acceptance_criteria>
  <done>Ranking/grouping/cap logic ported to a standalone module with parity tests; ready to drive cmdk.</done>
</task>

<task type="auto">
  <name>Task 2: Build the cmdk CommandPalette, wire real navigation, retire SearchPalette</name>
  <files>frontend/src/shell/CommandPalette.tsx, frontend/src/styles/shell.css, frontend/src/views/SearchPalette.tsx, frontend/src/views/search.css</files>
  <read_first>
    - .planning/phases/02-app-shell-routing-canvas-infrastructure/02-RESEARCH.md — Pattern 4 (cmdk Command.Dialog + shouldFilter={false} + externally-ranked items), "Don't Hand-Roll" (Command.Dialog gives focus-trap/restore + a11y roles for free), assumption A2 (keep the app's exact-substring ranking; do not adopt cmdk fuzzy)
    - .planning/phases/02-app-shell-routing-canvas-infrastructure/02-PATTERNS.md — CommandPalette section: the result-selection callback to replace (App.tsx lines 66-71 → navigate to the lineage route with sel/col) and the keyboard-nav to retire (SearchPalette lines 139-144)
    - .planning/phases/02-app-shell-routing-canvas-infrastructure/02-UI-SPEC.md — palette tokens (`--palette-bg: var(--color-surface-3)`, `--palette-width`, `--palette-max-height`, z-index 100), the no-results copy "No matches for \"{query}\".", and the carried-forward `.sp-*` row treatment
    - frontend/src/shell/CommandPalette.tsx — the 02-04 stub (open/onOpenChange props + Command.Dialog scaffold) this task fills
    - frontend/src/shell/AppShell.tsx — how the palette open-state + Cmd+K listener + rail-bottom trigger are already wired (02-04); this task consumes that open-state, it does not re-add the listener
    - frontend/src/selection/useSelection.ts + a lineage route from src/routes/lineage — the navigation target shape (to/params + search sel/col)
  </read_first>
  <action>
    Fill `src/shell/CommandPalette.tsx` (the 02-04 stub) using `cmdk`'s `Command.Dialog` bound to the AppShell-owned open state (`open`/`onOpenChange`), with `shouldFilter={false}` and `label="Search"`. Render a `Command.Input` (placeholder "Search tables, columns, notebooks, code…"), and drive `Command.List`'s children from `search()`/`GROUP_ORDER`/`GROUP_LABEL`/`MAX_PER_GROUP` (from Task 1) as `Command.Group`/`Command.Item`. Guard on `query.trim() !== ''` to render nothing below the input with no query; render `Command.Empty` with the exact copy `No matches for "{query}".` when a query has no results. On `onSelect` of a result, perform a REAL navigation (per 02-PATTERNS.md): a table/column result navigates to the lineage route with `?sel`/`?col` set (via `navigate({ to, params, search })` — a real navigation that pushes history, NOT a selection-only replace, since it changes which canvas/level is shown); a notebook/code result routes to the lineage canvas. Reuse the carried-forward `.sp-*` row/highlight treatment (move it into `shell.css` under the palette tokens; `--palette-bg` is surface-3). Do NOT re-add a manual `document.addEventListener('keydown')` or manual Arrow/Enter/Escape handling — cmdk + Radix Dialog own keyboard nav, focus-trap, and focus-restore (NAV-03). Render `hl()` output and matched code-line context as JSX children (React-escaped), never via dangerouslySetInnerHTML.
    Delete `src/views/SearchPalette.tsx` and `src/views/search.css` (retired with the old shell, D-17) — confirm nothing still imports them (App.tsx is already gone; the palette is now the shell's).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <human-check>Run `npm run dev`; confirm in BOTH themes: (1) Cmd+K and the rail-bottom search trigger both open the palette; (2) typing filters into grouped Tables/Columns/Notebooks/Code results in the same order/cap as before; (3) arrow keys move selection, Enter opens the result and lands on the lineage canvas with it selected, Esc closes and returns focus to where it was; (4) a no-match query shows 'No matches for "{query}".'.</human-check>
  </verify>
  <acceptance_criteria>
    - `src/shell/CommandPalette.tsx` uses `Command.Dialog` with `shouldFilter={false}`
    - `CommandPalette.tsx` contains no `document.addEventListener('keydown'` and no manual `ArrowDown`/`ArrowUp` handler (keyboard nav comes from cmdk)
    - The no-results branch renders `No matches for "{query}".`
    - On select, a table/column result calls `navigate(` to the lineage route with `search` carrying `sel`/`col`
    - `frontend/src/views/SearchPalette.tsx` no longer exists (`test ! -f frontend/src/views/SearchPalette.tsx`)
    - `cd frontend && npm run build` exits 0 (no dangling import of the deleted palette)
  </acceptance_criteria>
  <done>cmdk-based, fully keyboard-operable palette searching tables/columns/notebooks/code with preserved ranking and real navigation on select; the hand-rolled SearchPalette retired.</done>
</task>

</tasks>

## Flagged edge-probe assumptions (surfaced, not dropped)

The spec-less edge probe returned `unclassified` (`unresolved`) rows for **NAV-01** ("Cmd+K opens a palette searching tables/columns/code") and **NAV-03** ("the palette is fully keyboard-operable"). Surfaced as explicit planner assumptions rather than dropped: NAV-01 is covered by the search() parity truths + the Cmd+K/rail-trigger open truth; NAV-03 is covered by the cmdk/Radix-Dialog keyboard-operability truth. The un-enumerated edge for NAV-03 is full keyboard-operability of the composed widget, which 02-RESEARCH.md's Test Map classifies as manual-only (best verified by actually tabbing/arrowing) — verified by the both-themes human-check.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Notebook code content → palette code-match rows | Notebook source text (already loaded in the model) is rendered as match context in palette rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Tampering | palette code-match / hl() rendering | medium | mitigate | Matched code lines and highlighted text render as JSX children (React auto-escapes) and `hl()` builds text nodes + `<mark>` elements — never a concatenated HTML string and never `dangerouslySetInnerHTML`, so notebook content cannot inject markup into the palette |
</threat_model>

<verification>
- `npx vitest run src/shell/__tests__/search.test.ts` green.
- `npm run build` green; `test ! -f frontend/src/views/SearchPalette.tsx`.
- Manual (both themes): Cmd+K + rail trigger open; grouped/capped ranking preserved; keyboard nav + focus-trap/restore work; select lands on canvas; no-match copy correct.
</verification>

<success_criteria>
Cmd+K command palette on cmdk searching tables/columns/notebooks/code (NAV-01), fully keyboard-operable via the primitive (NAV-03), preserving the app's ranking and navigating to the canvas on select; the hand-rolled SearchPalette retired.
</success_criteria>

<output>
Create `.planning/phases/02-app-shell-routing-canvas-infrastructure/02-06-SUMMARY.md` when done.
</output>

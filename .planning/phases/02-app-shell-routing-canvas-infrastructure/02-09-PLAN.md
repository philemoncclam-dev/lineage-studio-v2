---
phase: 02-app-shell-routing-canvas-infrastructure
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/todos/pending/phase4-graph-mode-drill-url-wiring.md
autonomous: true
gap_closure: true
requirements: [SHELL-05, SHELL-06]
must_haves:
  truths:
    - "REQUIREMENTS.md and ROADMAP.md honestly reflect that the Knowledge-Graph drill URLs (/graph/$workspace/$lakehouse/$table) and resolvePathSegments wiring are DEFERRED to Phase 4 — SHELL-05/SHELL-06 no longer claim unqualified 'Complete' for the graph-mode drill hierarchy (Gap #2/#3, scope decision B)"
    - "The genuinely-satisfied portions stay marked satisfied: mode routes (/graph, /lineage, /purview), the /lineage/$workspace/$lakehouse/$table route, and selection (?sel/?col with replace:true)"
    - "A Phase-4 carry-forward todo captures exactly what Phase 4 must wire (resolvePathSegments beforeLoad on /graph/$workspace*, D-09 nearest-ancestor redirect + notice, URL-driven drill via navigate())"
    - "frontend/src/resolve/resolvePathSegments.ts and its tests remain in the tree, documented as intentionally staged for Phase 4 — NOT deleted or wired now"
  artifacts:
    - ".planning/REQUIREMENTS.md — SHELL-05/SHELL-06 status re-scoped to Partial with a one-line Phase-4 deferral reason"
    - ".planning/ROADMAP.md — Phase 2 SC#3 graph-mode deferral note + Phase 4 carry-forward note"
    - ".planning/todos/pending/phase4-graph-mode-drill-url-wiring.md — new carry-forward todo (resolves_phase: 04)"
  key_links:
    - "todo resolves_phase: 04 → Phase 4 (Knowledge Graph Canvas Rebuild) planning picks up graph-mode drill URL wiring so SHELL-05/06 graph-mode is satisfied there"
---

<objective>
Make the documentation honest about Gaps #2 and #3 (scope decision B — DEFERRED to Phase 4, docs/status only, no app code).

Verified reality (02-VERIFICATION.md Gap #2/#3, 02-REVIEW.md WR-01): the `/graph/$workspace/$lakehouse/$table` routes exist and are refresh-safe, but `GraphRouteView` ignores their path params and `GraphView`'s drill state is local-only and never calls `navigate()`, so every `/graph/*` URL renders the Estate view and graph-mode drilling never updates the URL or browser history. `resolvePathSegments` (name→GUID resolution + D-09 nearest-ancestor redirect + notice) is fully built and unit-tested but wired into no route. Yet REQUIREMENTS.md and ROADMAP.md mark SHELL-05/SHELL-06 "Complete" without qualification.

Rationale for deferral (locked, do not relitigate): Phase 4 rebuilds `GraphView` entirely on sigma.js + graphology with Estate→Workspace→Lakehouse→Table drill-down as a core success criterion (GRAPH-02). Wiring the current throwaway canvas's internal drill state to the URL now is discardable work; Phase 2 explicitly scoped GraphView-internal edits out (D-14/D-15). `resolvePathSegments` and its tests are staged for Phase 4 to consume — they stay in the tree.

Purpose: Close the "unmarked discrepancy between REQUIREMENTS/ROADMAP and the actual code" the verifier called out, and hand Phase 4 a precise carry-forward brief. No frontend source changes in this plan.
Output: Re-scoped SHELL-05/SHELL-06 status, a Phase-2 SC#3 deferral note, a Phase-4 carry-forward note, and a pending todo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-app-shell-routing-canvas-infrastructure/02-VERIFICATION.md
@.planning/phases/02-app-shell-routing-canvas-infrastructure/02-REVIEW.md
@.planning/phases/02-app-shell-routing-canvas-infrastructure/02-CONTEXT.md
</context>

<artifacts_produced>
New/changed artifacts this plan produces (docs/status only — NO frontend source):
- Changed: `.planning/REQUIREMENTS.md` — SHELL-05 and SHELL-06 traceability rows + checklist qualified (Partial, graph-mode drill deferred to Phase 4).
- Changed: `.planning/ROADMAP.md` — Phase 2 Success Criterion #3 gains a graph-mode deferral note; Phase 4 gains a carry-forward note referencing the todo.
- New: `.planning/todos/pending/phase4-graph-mode-drill-url-wiring.md` — carry-forward todo with `resolves_phase: 04`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Re-scope SHELL-05 / SHELL-06 status honestly in REQUIREMENTS.md and ROADMAP.md</name>
  <read_first>
    - .planning/REQUIREMENTS.md (SHELL-05/06 checklist lines ~46-47; traceability rows ~176-177 marked "Complete")
    - .planning/ROADMAP.md (Phase 2 Success Criteria, esp. #3 "destination, drill path, selection URL-addressable … back/forward moves through drill-down levels", line ~124)
    - .planning/phases/02-app-shell-routing-canvas-infrastructure/02-VERIFICATION.md (Gap #2/#3 — the exact satisfied-vs-deferred split)
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
    Use scoped edits (do not rewrite whole files). Make the status reflect what the verifier confirmed: mode routes + the `/lineage/$workspace/$lakehouse/$table` route + selection (`?sel`/`?col`, `replace:true`) are genuinely satisfied; the Knowledge-Graph drill hierarchy (`/graph/$workspace/$lakehouse/$table` params driving the view, and `resolvePathSegments` wiring / D-09 redirect at runtime) is deferred to Phase 4.

    In `.planning/REQUIREMENTS.md`:
    - In the Traceability table, change the SHELL-05 and SHELL-06 rows from `Complete` to a qualified status such as `Partial` (Phase column: keep `Phase 2`, and add a Phase 4 reference for the deferred portion) — the row must make the deferral legible, e.g. "Partial — graph-mode drill hierarchy + resolvePathSegments wiring deferred to Phase 4".
    - In the `### App Shell (SHELL)` checklist, append a one-line qualifier to SHELL-05 and SHELL-06 naming the deferred graph-mode drill portion and its Phase-4 owner and reason (Phase 4 rebuilds GraphView; wiring the throwaway canvas now is discardable). Keep the satisfied portions accurately described; do not claim the graph-mode drill works today.
    - Keep SHELL-07's status accurate too — it is being fixed by plan 02-07 (the crash); do not touch its wording here unless it currently over-claims.

    In `.planning/ROADMAP.md`:
    - Add a short deferral note to Phase 2 Success Criterion #3 (or immediately beneath the Phase 2 success-criteria block) stating that the Knowledge-Graph drill hierarchy's URL-addressability + back/forward is deferred to Phase 4 (GRAPH-02 rebuilds it), while mode routes, the lineage-table route, and selection are satisfied. One or two lines, honest, no scope inflation.

    Do NOT change any requirement's fundamental intent, do NOT mark anything newly Complete, and do NOT touch frontend source. This is a status-honesty edit only.
  </action>
  <verify>
    <automated>cd "C:/Users/user/Downloads/github/datalineage" && grep -n "SHELL-05\|SHELL-06" .planning/REQUIREMENTS.md | grep -iv "complete" | grep -i "phase 4\|deferred\|partial"</automated>
  </verify>
  <acceptance_criteria>
    - SHELL-05 and SHELL-06 no longer read as unqualified "Complete" in REQUIREMENTS.md; both traceability rows and checklist entries name the graph-mode drill + resolvePathSegments-wiring deferral to Phase 4 with a one-line reason.
    - The genuinely-satisfied portions (mode routes, /lineage/$workspace/$lakehouse/$table, selection) remain accurately marked satisfied.
    - ROADMAP.md Phase 2 SC#3 carries a short, honest graph-mode deferral note pointing at Phase 4 / GRAPH-02.
    - No frontend source files changed by this task.
  </acceptance_criteria>
  <done>REQUIREMENTS.md and ROADMAP.md honestly reflect the Phase-4 deferral of graph-mode drill URL-addressability; satisfied portions stay marked satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Create the Phase-4 carry-forward todo and document resolvePathSegments as intentionally staged</name>
  <read_first>
    - $HOME/.claude/gsd-core/workflows/add-todo.md (todo frontmatter/schema convention, incl. resolves_phase)
    - .planning/phases/02-app-shell-routing-canvas-infrastructure/02-VERIFICATION.md (the exact "missing" wiring items for Gap #2/#3)
    - .planning/phases/02-app-shell-routing-canvas-infrastructure/02-CONTEXT.md (D-07/D-09 — name→GUID resolution + nearest-ancestor redirect + notice behavior Phase 4 must deliver)
    - .planning/ROADMAP.md (Phase 4 goal + GRAPH-02 success criterion — the drill-down this wiring feeds)
  </read_first>
  <files>.planning/todos/pending/phase4-graph-mode-drill-url-wiring.md</files>
  <action>
    Create the pending-todo directory if absent and write `.planning/todos/pending/phase4-graph-mode-drill-url-wiring.md`. Match the todo frontmatter convention in add-todo.md; include `resolves_phase: 04` and `status: pending`, a title, a created date, and a source reference (this gap-closure plan / 02-VERIFICATION.md Gap #2/#3). Give the todo body a precise Phase-4 brief:

    - Wire `resolvePathSegments` into a `beforeLoad` on the `/graph/$workspace*` routes: resolve readable name segments to Purview-GUID node ids against the root-loaded graph, and implement the D-09 nearest-ancestor `redirect({ replace: true })` with the bounded `unresolved` notice for segments that don't resolve (no redirect loop, no hard error — Pitfall 4).
    - Drive the rebuilt (sigma.js + graphology) graph drill-down from the URL: the resolved path segments select the drill level, and drilling by clicking a node calls `navigate()` so drill levels are URL-addressable and back/forward walks them (SHELL-05/SHELL-06 graph-mode; GRAPH-02).
    - Note that `frontend/src/resolve/resolvePathSegments.ts` and `frontend/src/resolve/__tests__/resolvePathSegments.test.ts` already exist (name→GUID + nearest-ancestor redirect + notice, 9 passing tests) and are INTENTIONALLY STAGED for this — Phase 4 consumes them; they were deliberately NOT deleted in Phase 2.
    - Add a short "known-deferred, do not lose" note listing WR-02 (GraphView force-sim callback not memoized — resets on canvas-search keystroke) and WR-05 (LineageView stale column highlight) as issues the Phase 3/4 canvas rebuilds should absorb, and WR-06 (silent sample-data fallback has no UI indicator) as a separately-deferred UX item.

    Do NOT modify, wire, or delete `resolvePathSegments.ts` or any frontend source. This task only records intent.
  </action>
  <verify>
    <automated>cd "C:/Users/user/Downloads/github/datalineage" && test -f .planning/todos/pending/phase4-graph-mode-drill-url-wiring.md && grep -q "resolves_phase: 04" .planning/todos/pending/phase4-graph-mode-drill-url-wiring.md && grep -qi "resolvePathSegments" .planning/todos/pending/phase4-graph-mode-drill-url-wiring.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/todos/pending/phase4-graph-mode-drill-url-wiring.md` exists with `resolves_phase: 04` and `status: pending` in frontmatter (matching add-todo.md's convention).
    - The todo body names the two Phase-4 wiring actions (resolvePathSegments beforeLoad + D-09 redirect/notice; URL-driven graph drill via navigate) and records that resolvePathSegments.ts + its tests are intentionally staged (not to be deleted).
    - The known-deferred note lists WR-02, WR-05 (Phase 3/4 canvas rebuilds) and WR-06 (separately deferred).
    - No frontend source files changed (resolvePathSegments.ts and all of frontend/src untouched).
  </acceptance_criteria>
  <done>Phase-4 carry-forward todo written with resolves_phase:04 and a precise wiring brief; resolvePathSegments documented as staged; no source touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation/status-only plan. No code, no inputs, no network calls, no auth surface, no secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09-01 | Repudiation / Integrity of record | planning docs | low | mitigate | The plan's whole point is to remove a dishonest "Complete" status; it improves record integrity. No adverse threat introduced. |
| T-02-09-SC | Tampering (supply chain) | npm installs | n/a | accept | No dependencies, no code; nothing to install or audit. |

Honest assessment: no security-relevant surface — this is a docs/status honesty edit plus a pending todo.
</threat_model>

<verification>
- `grep` confirms SHELL-05/SHELL-06 in REQUIREMENTS.md are no longer unqualified "Complete" and name the Phase-4 deferral.
- `.planning/todos/pending/phase4-graph-mode-drill-url-wiring.md` exists with `resolves_phase: 04` and references resolvePathSegments.
- `git status` shows only `.planning/` docs changed — no `frontend/src` files touched.
</verification>

<success_criteria>
- REQUIREMENTS.md and ROADMAP.md honestly reflect the Phase-4 deferral of graph-mode drill URL-addressability; satisfied portions stay satisfied.
- A precise Phase-4 carry-forward todo exists; resolvePathSegments stays staged in-tree.
- Zero frontend source changes.
</success_criteria>

<output>
Create `.planning/phases/02-app-shell-routing-canvas-infrastructure/02-09-SUMMARY.md` when done.
</output>
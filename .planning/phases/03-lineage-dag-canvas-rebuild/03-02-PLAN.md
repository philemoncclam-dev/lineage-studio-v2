---
phase: 03-lineage-dag-canvas-rebuild
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/test/setup.ts
  - frontend/src/styles/components.css
autonomous: true
requirements: [DAG-01, TRUST-01]
must_haves:
  truths:
    - "@xyflow/react@12.11.2 and @dagrejs/dagre@3.0.0 are installed at exact pinned versions and the retired reactflow@11 package is removed — the renamed-package v11→v12 migration to @xyflow/react + dagre (D-01)"
    - "The frontend Vitest/jsdom environment can mount an <ReactFlow> instance without crashing — ResizeObserver, DOMMatrixReadOnly, and SVGElement.prototype.getBBox are polyfilled in test/setup.ts"
    - "components.css declares the new tier-3 lineage tokens (node geometry, provenance dasharray, trace/dim) — every value resolves to a Phase-1 tier-2 semantic or a sanctioned component-geometry literal; no raw hue/hex is introduced"
    - "npm run audit:tokens still passes after the new tier-3 tokens are added (no cross-channel collision, contrast, or CVD regression)"
  artifacts:
    - frontend/package.json (@xyflow/react + @dagrejs/dagre pinned, reactflow removed)
    - frontend/src/test/setup.ts (ResizeObserver/DOMMatrixReadOnly/getBBox jsdom mocks)
    - frontend/src/styles/components.css (tier-3 --dag-* + --lineage-toolbar-* tokens)
  key_links:
    - "test/setup.ts mocks -> every Vitest that mounts <ReactFlow> (plans 03-05 LineageEdge.test, 03-07 view tests)"
    - "components.css --dag-* tokens -> lineage-dag.css + custom node/edge components (plan 03-05)"
  prohibitions:
    - "Do not import the full themed xyflow stylesheet (dist/style.css) anywhere — it ships default light-grey/blue hex that fights the OKLCH token audit; the base-only layout reset is the sanctioned import (RESEARCH anti-pattern)"
    - "No new primitive colour, spacing, or type token — tier-3 only, resolving to existing tier-2 semantics (01-UI-SPEC per-canvas rule)"
    - "No raw hex or raw px hue values in components.css additions (token discipline, 01-REVIEW enforced)"
---

<objective>
Lay the toolchain + token foundation the rest of Phase 3 builds on: swap the stale `reactflow@11` dependency for the locked `@xyflow/react@12.11.2` + `@dagrejs/dagre@3.0.0`, add the three jsdom polyfills xyflow needs to render under Vitest (Wave 0 test-infra gap), and declare this phase's new tier-3 component tokens in `components.css`.

Purpose: Every downstream wave (pure layout, node/edge components, view integration) needs the packages installed, the test harness able to mount `<ReactFlow>`, and the geometry/provenance/trace tokens present. This plan removes all three blockers in one foundation pass.
Output: Updated `package.json`/lockfile, extended `test/setup.ts`, and the tier-3 token block.
</objective>

<artifacts_produced>
New symbols this plan creates (source-grounding exclusion list):
- Dependency: `@xyflow/react@12.11.2`, `@dagrejs/dagre@3.0.0` (removed: `reactflow`)
- test/setup.ts globals: `ResizeObserver` mock, `DOMMatrixReadOnly` mock, `SVGElement.prototype.getBBox` mock
- CSS custom properties: `--lineage-toolbar-bg`, `--lineage-toolbar-border`, `--dag-node-width`, `--dag-node-header-height`, `--dag-node-row-height`, `--dag-node-max-height`, `--dag-edge-dasharray-inferred`, `--dag-edge-dasharray-declared`, `--dag-dim-opacity`, `--dag-trace-stroke-width`, `--dag-base-stroke-width`, `--dag-trace-transition`
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-lineage-dag-canvas-rebuild/03-RESEARCH.md
@.planning/phases/03-lineage-dag-canvas-rebuild/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap reactflow → @xyflow/react + @dagrejs/dagre (exact pins)</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <read_first>
    - frontend/package.json (dependencies — `reactflow` ^11.11.4 is present and imported nowhere in src, per UI-SPEC's grep; this is a clean swap)
    - .planning/phases/03-lineage-dag-canvas-rebuild/03-RESEARCH.md § "Standard Stack" (installation block, version-pin rationale) and § "Package Legitimacy Audit" (both packages Approved — no [ASSUMED]/[SUS], so no legitimacy checkpoint is required)
  </read_first>
  <action>
    In frontend/, run `npm uninstall reactflow` then `npm install --save-exact @xyflow/react@12.11.2 @dagrejs/dagre@3.0.0`. Pin both to the exact versions (no caret) given how recently dagre 3.0.0 shipped (RESEARCH version note). Confirm `reactflow` is gone from package.json dependencies and no `from 'reactflow'` import exists anywhere in src.
  </action>
  <verify>
    <automated>cd frontend && node -e "const p=require('./package.json');const d=p.dependencies;if(d.reactflow)throw new Error('reactflow still present');if(d['@xyflow/react']!=='12.11.2')throw new Error('xyflow pin wrong: '+d['@xyflow/react']);if(d['@dagrejs/dagre']!=='3.0.0')throw new Error('dagre pin wrong: '+d['@dagrejs/dagre']);console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: package.json dependencies contain `@xyflow/react` exactly `12.11.2` and `@dagrejs/dagre` exactly `3.0.0`, and no `reactflow` key.
    - Behavior assertion: `grep -RIn "from 'reactflow'" frontend/src` returns no matches (nothing imported the old package).
    - Install integrity: `node_modules/@xyflow/react` and `node_modules/@dagrejs/dagre` exist.
  </acceptance_criteria>
  <done>reactflow removed, both new packages installed at exact pins, no stale imports.</done>
</task>

<task type="auto">
  <name>Task 2: Add the xyflow-under-jsdom polyfills to test/setup.ts</name>
  <files>frontend/src/test/setup.ts</files>
  <read_first>
    - frontend/src/test/setup.ts (currently one line: `import '@testing-library/jest-dom'`)
    - frontend/vitest.config.ts (confirms setup.ts is the configured setupFiles)
    - .planning/phases/03-lineage-dag-canvas-rebuild/03-RESEARCH.md § "Validation Architecture › New setup requirement" (the three required mocks) and § "Wave 0 Gaps"
    - .planning/phases/03-lineage-dag-canvas-rebuild/03-PATTERNS.md § "frontend/src/test/setup.ts (extended, in-place)"
  </read_first>
  <action>
    Append to test/setup.ts, below the existing jest-dom import: a `ResizeObserver` class polyfill (no-op `observe`/`unobserve`/`disconnect`) assigned to `globalThis.ResizeObserver`; a `DOMMatrixReadOnly` class polyfill sufficient for xyflow's transform reads (constructor accepting an optional transform string, `m22`/`m11` numeric fields) assigned to `globalThis.DOMMatrixReadOnly`; and a `SVGElement.prototype.getBBox` stub returning `{ x:0, y:0, width:0, height:0 }`. Guard each with an existence check so a real jsdom implementation, if present, is not clobbered. These are plain globalThis polyfills — no external dependency.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/test/setup.ts --reporter=dot --passWithNoTests 2>&1 | tail -5; node -e "const s=require('fs').readFileSync('src/test/setup.ts','utf8');for(const t of ['ResizeObserver','DOMMatrixReadOnly','getBBox'])if(!s.includes(t))throw new Error('missing '+t);console.log('mocks present')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: setup.ts references `ResizeObserver`, `DOMMatrixReadOnly`, and `getBBox`.
    - Behavior assertion: a throwaway smoke test that mounts a trivial `<ReactFlow nodes={[]} edges={[]} />` under jsdom does not throw a ResizeObserver/getBBox error (validated when 03-05's LineageEdge.test runs; setup is the enabling change here).
  </acceptance_criteria>
  <done>The three mocks exist and are guarded; the Vitest suite still runs green.</done>
</task>

<task type="auto">
  <name>Task 3: Declare the tier-3 lineage-DAG component tokens</name>
  <files>frontend/src/styles/components.css</files>
  <read_first>
    - frontend/src/styles/components.css (existing tier-3 block; the `.seg`/`.ls-*`/`.col`/`.xform`/`.flow-item` rules that already consume tier-2 semantics; `--row-height` primitive)
    - .planning/phases/03-lineage-dag-canvas-rebuild/03-UI-SPEC.md § "New Tier-3 Component Tokens" (the exact token list + values) and § "Spacing Scale" (component-geometry literal exceptions)
    - frontend/scripts/audit-tokens.mjs (the collision/contrast/CVD gate that must stay green)
  </read_first>
  <action>
    Add the tier-3 token block from UI-SPEC "New Tier-3 Component Tokens" to components.css: `--lineage-toolbar-bg: var(--color-surface-1)`, `--lineage-toolbar-border: var(--color-border)`, `--dag-node-width: 240px`, `--dag-node-header-height: 40px`, `--dag-node-row-height: var(--row-height)`, `--dag-node-max-height: 320px`, `--dag-edge-dasharray-inferred: 5 4`, `--dag-edge-dasharray-declared: none`, `--dag-dim-opacity: 0.15`, `--dag-trace-stroke-width: 2.5px`, `--dag-base-stroke-width: 1.5px`, `--dag-trace-transition: 180ms`. Every colour-bearing token resolves to an existing tier-2 semantic (surface/border); the px/dasharray values are sanctioned component-geometry literals, not new hues. Do not add any raw hex or any new edge-type/domain colour token.
  </action>
  <verify>
    <automated>cd frontend && npm run audit:tokens && node -e "const c=require('fs').readFileSync('src/styles/components.css','utf8');for(const t of ['--dag-node-width','--dag-edge-dasharray-inferred','--dag-dim-opacity','--lineage-toolbar-bg'])if(!c.includes(t))throw new Error('missing '+t);console.log('tokens present')"</automated>
  </verify>
  <acceptance_criteria>
    - CLI output: `npm run audit:tokens` exits 0 (no new collision/contrast/CVD failure introduced).
    - Source assertion: all twelve `--dag-*`/`--lineage-toolbar-*` tokens are present in components.css; none introduce a raw hex value (`grep -nE '#[0-9a-fA-F]{3,8}' ` over the added block returns nothing).
  </acceptance_criteria>
  <done>Tier-3 tokens declared, resolving to tier-2 semantics; token audit green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules (supply chain) | Two new packages enter the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering (supply chain) | npm install @xyflow/react, @dagrejs/dagre | high | mitigate | RESEARCH § Package Legitimacy Audit verified both live against the npm registry (age, download volume, source repo, no postinstall scripts) — both `OK / Approved`, neither `[ASSUMED]`/`[SUS]`/`[SLOP]`, so no blocking legitimacy checkpoint is required. Exact-version pins prevent a later silent major bump. |
| T-03-04 | Tampering (style bleed) | xyflow default stylesheet | medium | mitigate | Prohibition recorded: the full themed `dist/style.css` (raw hex) is never imported; only the base layout reset is used, keeping the OKLCH token audit authoritative. |
</threat_model>

<verification>
- `cd frontend && npm run test:run` green (existing suites unaffected by the setup.ts additions).
- `cd frontend && npx tsc -b --noEmit` clean.
- `npm run audit:tokens` exits 0.
</verification>

<success_criteria>
- reactflow removed; @xyflow/react + @dagrejs/dagre pinned exactly.
- The three jsdom mocks are present so downstream `<ReactFlow>` tests can mount.
- Tier-3 lineage tokens declared with no token-audit regression.
</success_criteria>

<flagged_assumptions>
Spec-less probe returned DAG-01 as `unclassified/unresolved`. This plan only lays its geometry-token + toolchain foundation; the real DAG-01 edge (deterministic LR node count/order) is resolved and tested in plan 03-03. TRUST-01's dasharray tokens are declared here; the independent-channel behaviour is tested in plan 03-05. No numeric boundary is owned by this foundation plan.
</flagged_assumptions>

<output>
Create `.planning/phases/03-lineage-dag-canvas-rebuild/03-02-SUMMARY.md` when done.
</output>

---
phase: 03-lineage-dag-canvas-rebuild
plan: 03
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - frontend/src/views/lineage-dag/types.ts
  - frontend/src/views/lineage-dag/trace.ts
  - frontend/src/views/lineage-dag/trace.test.ts
  - frontend/src/views/lineage-dag/useDagreLayout.ts
  - frontend/src/views/lineage-dag/useDagreLayout.test.ts
  - frontend/src/views/lineage-dag/toXyflow.ts
  - frontend/src/views/lineage-dag/toXyflowEdges.test.ts
autonomous: true
requirements: [DAG-01, DAG-02, DAG-03, DAG-06, DAG-07]
must_haves:
  truths:
    - "buildDagreLayout(tables, notebooks, ops, mode) lays a fixture graph out left-to-right (rankdir LR): a downstream table's x is strictly greater than its upstream table's x (DAG-01)"
    - "Node count in the layout equals tables.length + notebooks.length; every id has an {x,y}"
    - "In Column mode a table node's height is 40 + min(columns.length, 10) * 28 capped at 320; in Table mode every table + notebook node height is 40 (DAG-06)"
    - "Calling buildDagreLayout twice with the same (graph, mode) returns byte-identical {x,y} for every node — no random seed, no warm-start (DAG-07)"
    - "dagre's center point is converted to xyflow's top-left position (x - width/2, y - height/2) before it becomes a Node.position (RESEARCH Pitfall 3)"
    - "In Column mode a column edge [fromKey,toKey] resolves sourceHandle=`${fromKey}__source` and targetHandle=`${toKey}__target`; in Table mode both fall back to `__node__source`/`__node__target` (DAG-02)"
    - "Object-level ops edges (reads/writes through a notebook) always use the `__node__*` fallback handle pair on both ends"
    - "Every edge toXyflow emits carries data.provenance === 'inferred' (D-09 — all edges dashed until Phase 5)"
    - "trace(colEdges, key) returns the full upstream+downstream connected Set and terminates on a cyclic colEdges array (visited-guard, ported verbatim from the retired LineageView.tsx) (DAG-03/DAG-04)"
    - { statement: "E1 canvas zero-one-many — dagre lays out 1 table through many deterministically; a zero-column table in Column mode gets the collapsed 40px height (min(0,10)*28 = 0), never a broken card", verification: backstop }
  artifacts:
    - frontend/src/views/lineage-dag/types.ts (TableNodeData/NotebookNodeData/LineageEdgeData + handle-id helpers)
    - frontend/src/views/lineage-dag/trace.ts (+ trace.test.ts)
    - frontend/src/views/lineage-dag/useDagreLayout.ts (+ useDagreLayout.test.ts)
    - frontend/src/views/lineage-dag/toXyflow.ts (+ toXyflowEdges.test.ts)
  key_links:
    - "AppModel (tables, notebooks, colEdges, ops) -> useDagreLayout -> toXyflow -> xyflow Node[]/Edge[] consumed by LineageDagView (plan 03-07)"
    - "types.ts handle-id helpers -> TableNode/NotebookNode components (plan 03-05) render the matching Handle ids"
  prohibitions:
    - "Do not reuse lineageLayout.ts's place() cursor {x,y} as final positions — its y-cursor stacking is not dagre's barycenter compaction (RESEARCH anti-pattern)"
    - "Do not pass dagre config as a second argument to dagre.layout() — config lives on g.setGraph({...}) (RESEARCH Pitfall/Assumption A1)"
    - "Do not read node.width/node.height as measured dims (v12 repurposed them as style overrides) — this module computes height itself from columns.length"
---

<objective>
Build the deterministic, fully-unit-tested layout + mapping core of the DAG canvas as pure functions — no React, no DOM: `buildDagreLayout` (real `dagre.layout()` over table/notebook/ops topology with per-mode node geometry, center→top-left conversion), `toXyflow` (topology + dagre positions → xyflow `Node[]`/`Edge[]` with correct per-row vs fallback handle-id resolution and hardcoded `inferred` provenance), the ported `trace()` BFS/DFS, and the shared `types.ts` (node/edge data shapes + handle-id helpers).

Purpose: This is the heart of DAG-01/02/06/07 correctness and the seam RESEARCH Open Question #1 resolves — dagre supersedes `lineageLayout.ts`'s placeholder placement for the DAG view while topology still flows from the Phase-2 source. Keeping it pure makes determinism (DAG-07) and handle resolution (DAG-02) directly assertable in Vitest without mounting a canvas.
Output: Four source modules + three colocated test files, all green.
</objective>

<artifacts_produced>
New symbols this plan creates (source-grounding exclusion list):
- `types.ts`: `TableNodeData`, `NotebookNodeData`, `LineageEdgeData`, `LineageMode` (`'table' | 'column'`); handle-id helpers `colSourceHandle(key)`, `colTargetHandle(key)`, constants `NODE_SOURCE_HANDLE` (`__node__source`), `NODE_TARGET_HANDLE` (`__node__target`); `tableIdOfColKey(key)`
- `trace.ts`: `trace(colEdges, key): Set<string>`
- `useDagreLayout.ts`: `buildDagreLayout(tables, notebooks, ops, mode): Map<string,{x,y}>`, `nodeHeight(mode, columnCount): number`
- `toXyflow.ts`: `toXyflowNodes(...)`, `toXyflowEdges(...)` (or a single `toXyflow(...)` returning `{ nodes, edges }`)
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-lineage-dag-canvas-rebuild/03-RESEARCH.md
@.planning/phases/03-lineage-dag-canvas-rebuild/03-UI-SPEC.md
@.planning/phases/03-lineage-dag-canvas-rebuild/03-PATTERNS.md
@frontend/src/model/lineageLayout.ts
@frontend/src/model/index.tsx
@frontend/src/data.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared types + handle-id helpers + ported trace()</name>
  <files>frontend/src/views/lineage-dag/types.ts, frontend/src/views/lineage-dag/trace.ts, frontend/src/views/lineage-dag/trace.test.ts</files>
  <read_first>
    - frontend/src/views/LineageView.tsx (the `trace()` function, lines 10-21 — port verbatim, same visited-guard BFS/DFS body)
    - frontend/src/data.ts (`Table`, `Col`, `NB` shapes; column `key` = `${tableId}.${colName}`)
    - .planning/phases/03-lineage-dag-canvas-rebuild/03-UI-SPEC.md § "Column-row edge anchoring" (the `${col.key}__source`/`__target` and `__node__*` id conventions)
    - .planning/phases/03-lineage-dag-canvas-rebuild/03-PATTERNS.md § "LineageDagView.tsx › Trace logic to port verbatim" and § "Shared Patterns › Trace-driven CSS class composition"
  </read_first>
  <behavior>
    - trace([['a','b'],['b','c']], 'b') returns a Set containing a, b, c (walks both upstream and downstream from b).
    - trace on a cyclic array (e.g. [['a','b'],['b','a']]) terminates and returns {a,b} (visited-guard prevents infinite recursion).
    - trace([...], key) for a key with no edges returns just {key}.
  </behavior>
  <action>
    Create types.ts exporting: `LineageMode` = `'table' | 'column'`; `TableNodeData` (id, name, layer, columns: Col[], mode: LineageMode, colorKey) ; `NotebookNodeData` (id, name); `LineageEdgeData` (kind: 'reads'|'writes'|'derives', provenance: 'declared'|'inferred', from?: string, to?: string); handle-id helpers `colSourceHandle(key) => `${key}__source``, `colTargetHandle(key) => `${key}__target``, constants `NODE_SOURCE_HANDLE = '__node__source'`, `NODE_TARGET_HANDLE = '__node__target'`, and `tableIdOfColKey(key) => key.slice(0, key.lastIndexOf('.'))`. Create trace.ts exporting `trace(colEdges: [string,string][], key: string): Set<string>` — port the retired LineageView.tsx body unchanged (the `go(c, dir)` recursion with the `set.has()` visited guard). Add trace.test.ts covering the three behaviors above.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/views/lineage-dag/trace.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `npx vitest run src/views/lineage-dag/trace.test.ts` passes.
    - Behavior assertion: cyclic-input case returns without hanging (visited-guard preserved).
    - Source assertion: types.ts exports the four handle-id symbols the 03-05 components will import.
  </acceptance_criteria>
  <done>trace() ported and green; shared types + handle-id convention exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: buildDagreLayout — real dagre, per-mode geometry, determinism (DAG-01/06/07)</name>
  <files>frontend/src/views/lineage-dag/useDagreLayout.ts, frontend/src/views/lineage-dag/useDagreLayout.test.ts</files>
  <read_first>
    - frontend/src/model/lineageLayout.ts (reuse the id/name/layer/columns shape-building and topology; REPLACE its `place()` cursor algorithm — do not port depth-longest-path or y-cursor)
    - .planning/phases/03-lineage-dag-canvas-rebuild/03-RESEARCH.md § "Architecture Patterns › Pattern 3" (setGraph config, center→top-left conversion) and § "Common Pitfalls › Pitfall 3"
    - .planning/phases/03-lineage-dag-canvas-rebuild/03-UI-SPEC.md § "Node geometry" table and § "Dagre tuning" block
    - .planning/phases/03-lineage-dag-canvas-rebuild/03-VALIDATION.md § "Per-Task Verification Map" (DAG-01/06/07 rows)
  </read_first>
  <behavior>
    - LR: for a fixture graph raw_orders → clean_orders, the clean_orders node's x is strictly greater than raw_orders's x.
    - node count: returned Map size === tables.length + notebooks.length.
    - mode geometry: nodeHeight('table', 4) === 40; nodeHeight('column', 4) === 40 + 4*28; nodeHeight('column', 20) === 320 (capped at 10 rows); nodeHeight('column', 0) === 40.
    - determinism: buildDagreLayout(g, 'column') called twice → deepEqual (byte-identical x/y).
  </behavior>
  <action>
    Create useDagreLayout.ts exporting `nodeHeight(mode, columnCount)` = mode==='table' ? 40 : 40 + Math.min(columnCount, 10) * 28 (naturally ≤ 320), and `buildDagreLayout(tables, notebooks, ops, mode)`: build a `new dagre.graphlib.Graph()`, `g.setDefaultEdgeLabel(() => ({}))`, `g.setGraph({ rankdir:'LR', ranksep:64, nodesep:32, edgesep:16, marginx:32, marginy:32 })` (config on setGraph, NOT as a layout() arg), `g.setNode(id, { width: 240, height })` per table (height via nodeHeight) and per notebook (height 40), `g.setEdge(s, t)` per ops pair, then `dagre.layout(g)`. Read back each node and convert center→top-left: `{ x: n.x - n.width/2, y: n.y - n.height/2 }` into a `Map<string,{x,y}>`. No random seed, no incremental state. Add useDagreLayout.test.ts covering all four behaviors, including the DAG-07 determinism deepEqual case.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/views/lineage-dag/useDagreLayout.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `npx vitest run src/views/lineage-dag/useDagreLayout.test.ts` passes with the LR, node-count, per-mode-geometry, and determinism cases.
    - Behavior assertion: the determinism test does a structural deepEqual of two layout calls (DAG-07).
    - Source assertion: dagre config appears inside `setGraph({...})`, not as a second argument to `layout()`.
  </acceptance_criteria>
  <done>Deterministic LR dagre layout with per-mode geometry and center→top-left conversion; DAG-01/06/07 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: toXyflow — Node[]/Edge[] builder with per-mode handle resolution (DAG-02)</name>
  <files>frontend/src/views/lineage-dag/toXyflow.ts, frontend/src/views/lineage-dag/toXyflowEdges.test.ts</files>
  <read_first>
    - frontend/src/model/adapt.ts (colEdges + ops shapes this consumes; xform map)
    - frontend/src/views/lineage-dag/types.ts (created in Task 1 — handle-id helpers, data types)
    - .planning/phases/03-lineage-dag-canvas-rebuild/03-UI-SPEC.md § "Column-row edge anchoring" (Column vs Table vs object-level handle rules) and § "Provenance & Edge-Type Channel" (D-09 all-inferred)
    - .planning/phases/03-lineage-dag-canvas-rebuild/03-VALIDATION.md § "Per-Task Verification Map" (DAG-02 row)
  </read_first>
  <behavior>
    - Column mode: a column edge [fromKey,toKey] emits an xyflow Edge with sourceHandle === `${fromKey}__source`, targetHandle === `${toKey}__target`, and source/target node ids === the owning table ids (via tableIdOfColKey).
    - Table mode: the same column edge emits sourceHandle === `__node__source`, targetHandle === `__node__target` (fallback pair), source/target still the owning table node ids.
    - Object-level ops edge (reads/writes): sourceHandle/targetHandle always `__node__source`/`__node__target`.
    - Every emitted edge has data.provenance === 'inferred'; edge type is set to the lineage edge component's registered type name.
    - Every table Node has type 'tableNode' with data.mode set to the requested mode; notebooks have type 'notebookNode'; positions come from the passed dagre Map.
  </behavior>
  <action>
    Create toXyflow.ts exporting `toXyflow(tables, notebooks, colEdges, ops, positions, mode)` returning `{ nodes, edges }`. Nodes: one per table (`type:'tableNode'`, `position: positions.get(id)`, `data: { ...TableNodeData, mode }`) and per notebook (`type:'notebookNode'`). Edges: for each colEdge, resolve node ids via `tableIdOfColKey`, and choose handles by mode — Column: `colSourceHandle(fromKey)`/`colTargetHandle(toKey)`; Table: `NODE_SOURCE_HANDLE`/`NODE_TARGET_HANDLE`. For each ops pair always use the `__node__*` fallback pair. Set every edge `type` to the lineageEdge registered type and `data: { kind, provenance: 'inferred', from, to }` (hardcode inferred per D-09). Add toXyflowEdges.test.ts covering the four edge behaviors above for both modes.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/views/lineage-dag/toXyflowEdges.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `npx vitest run src/views/lineage-dag/toXyflowEdges.test.ts` passes.
    - Behavior assertion: the same fixture column edge produces `${key}__source`/`__target` in Column mode and `__node__source`/`__node__target` in Table mode (DAG-02 + toggle handle-fallback).
    - Behavior assertion: every emitted edge's `data.provenance` is `'inferred'` (D-09).
  </acceptance_criteria>
  <done>Nodes/edges builder resolves handles correctly per mode; all edges inferred; DAG-02 test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AppModel (already-fetched, in-memory) → pure layout functions | No new external input; operates on data already validated at the fetch boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Tampering (non-determinism) | buildDagreLayout | medium | mitigate | Layout is a pure function of (graph, mode) with no random seed or warm-start; the DAG-07 determinism test asserts byte-identical output across calls, closing the "re-jump" risk that would undermine trust in the canvas. |
| T-03-06 | Information Disclosure (XSS via data) | column/notebook names flowing into node data | low | transfer | Names are carried as plain data here; they are rendered as JSX text nodes (auto-escaped) only in plan 03-05's components — no raw-HTML path exists. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/views/lineage-dag --reporter=dot` green (trace, layout, toXyflow).
- `cd frontend && npx tsc -b --noEmit` clean.
</verification>

<success_criteria>
- Deterministic LR dagre layout with per-mode geometry (DAG-01/06/07) proven by test.
- Handle-id resolution correct for Column, Table, and object-level edges (DAG-02).
- trace() ported and cycle-safe (DAG-03/04 algorithm); all edges inferred (D-09).
</success_criteria>

<flagged_assumptions>
Spec-less probe rows resolved here:
- DAG-01/03/07 (`unclassified/unresolved`) → real edges are deterministic LR node order (DAG-01), cycle-safe trace Set (DAG-03), and byte-identical positions across renders (DAG-07) — all covered by Task 1/2 tests.
- DAG-06 (`boundary`/`precision` — flagged FALSE POSITIVE for a view toggle with no numeric contract) → confirmed no numeric boundary; the real DAG-06 edge is per-mode node height + handle-fallback recomputation, covered by Task 2/3. The classifier's boundary/precision hits are dismissed as spurious, not silently dropped.
- DAG-02 (`unclassified`) → real edge is exact `${col.key}__source/__target` vs `__node__*` handle resolution, covered by Task 3.
</flagged_assumptions>

<output>
Create `.planning/phases/03-lineage-dag-canvas-rebuild/03-03-SUMMARY.md` when done.
</output>

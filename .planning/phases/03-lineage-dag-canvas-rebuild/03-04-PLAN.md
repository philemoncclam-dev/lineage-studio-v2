---
phase: 03-lineage-dag-canvas-rebuild
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - frontend/src/model/adapt.ts
  - frontend/src/model/index.tsx
  - frontend/src/shell/Inspector.tsx
  - frontend/src/shell/__tests__/Inspector.test.tsx
autonomous: true
requirements: [DAG-05, TRUST-02]
must_haves:
  truths:
    - "When a column is selected (useSelection().col set), the Inspector renders a ColumnCard with, in order: a provenance line (dashed swatch + 'Inferred'), Transform, Source → Target, Evidence, and Connections (DAG-05)"
    - "The Transform section shows the transform expression in the .xform code block plus the frontend-synthesized plain-English sentence from adapt.ts's xform map; for a pass-through (no transform) the code block is omitted and only the sentence renders (missing-field-omits-its-row)"
    - "The Evidence section shows the matched notebook, cell index, and line as plain text, the verbatim snippet in a mono code block, and the locked caption 'Inferred from static pattern-matching — not executed.' (TRUST-02, D-13)"
    - "When a selected column has no resolvable evidence, the Evidence section is omitted entirely — never a rendered-blank block (missing-evidence fallback)"
    - "adapt.ts threads m.evidence into an AppModel.evidence map keyed by target column key, while the plain-English synthesis stays exactly as-is (D-13 — synthesis never moves to the backend)"
    - "The Connections line reads 'Upstream N · Downstream N' from the column's directional colEdge neighbourhood"
    - "All snippet/notebook text renders as escaped JSX text nodes — no raw-HTML injection path exists in the Inspector (canon phase-XSS mitigation)"
    - { statement: "E6 inspector partial — a null transform omits the code block and renders only the plain-English sentence; missing evidence omits the Evidence section", verification: backstop }
  artifacts:
    - frontend/src/model/adapt.ts (evidence map construction)
    - frontend/src/model/index.tsx (AppModel.evidence field + sampleModel default)
    - frontend/src/shell/Inspector.tsx (ColumnCard + column branch in resolveSelected)
    - frontend/src/shell/__tests__/Inspector.test.tsx (ColumnCard coverage)
  key_links:
    - "backend ColumnMap.evidence (03-01) -> api.ts ColumnMap.evidence -> adapt.ts AppModel.evidence -> Inspector ColumnCard Evidence section"
    - "useSelection().col -> Inspector resolveSelected column branch -> ColumnCard"
  prohibitions:
    - "The plain-English transform explanation stays frontend-synthesized in adapt.ts exactly as today — never re-synthesized in the Inspector or moved to the backend (D-13)"
    - "The Evidence caption is locked copy and must not be reworded to imply semantic/executed understanding (pitfall #6)"
    - "No raw-HTML injection when rendering the snippet or any notebook source — JSX text nodes only (React auto-escaping is the control)"
---

<objective>
Fill DAG-05 and TRUST-02's inspector surface: extend `adapt.ts` to thread the new backend `evidence` into an `AppModel.evidence` map, and extend `Inspector.tsx` with a `ColumnCard` that renders — when a column is selected — the provenance line, Transform (reusing the already-defined-but-unused `.xform` classes), Source → Target (`.flow-item`), the new Evidence section (verbatim snippet + locked caption), and Connections.

Purpose: This is where the honest, regex-parsed evidence behind an inferred edge becomes visible (pitfall #6) and where a column's transform + inputs/outputs are explained. It reuses Phase-2 Inspector mechanics and Phase-1 CSS wholesale — the net-new work is the column resolution branch and the Evidence block, not a redesign.
Output: Evidence-threaded adapt, AppModel.evidence, ColumnCard, and component-test coverage.
</objective>

<artifacts_produced>
New symbols this plan creates (source-grounding exclusion list):
- `AppModel.evidence: Record<string, ColumnMapEvidence>` (model/index.tsx) + sampleModel default
- `adapt.ts` evidence map construction inside the write-edge column loop
- `Inspector.tsx`: `ColumnCard` component; `ResolvedColumn` branch in `resolveSelected`
- Inspector test cases: ColumnCard Transform/Source→Target/Evidence rendering, missing-evidence omit, pass-through code-block omit
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-lineage-dag-canvas-rebuild/03-UI-SPEC.md
@.planning/phases/03-lineage-dag-canvas-rebuild/03-PATTERNS.md
@frontend/src/api.ts
@frontend/src/model/adapt.ts
@frontend/src/model/index.tsx
@frontend/src/shell/Inspector.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread evidence into adapt.ts + AppModel</name>
  <files>frontend/src/model/adapt.ts, frontend/src/model/index.tsx</files>
  <read_first>
    - frontend/src/model/adapt.ts (the write-edge column loop, lines 39-55 — the xform-map construction to enrich, not rebuild)
    - frontend/src/model/index.tsx (the `AppModel` interface + `sampleModel()`; add the new map field additively)
    - frontend/src/api.ts (`ColumnMap.evidence` + `ColumnMapEvidence` from plan 03-01)
    - .planning/phases/03-lineage-dag-canvas-rebuild/03-PATTERNS.md § "frontend/src/model/adapt.ts (extended, in-place)" and § "Additive, optional backend field"
  </read_first>
  <action>
    In model/index.tsx add `evidence: Record<string, ColumnMapEvidence>` to `AppModel` (import the type from '../api') and default it to an empty object in `sampleModel()`. In adapt.ts, inside the existing per-column loop, after `toCol` resolves, record `evidence[toCol.key] = m.evidence` when `m.evidence` is present; build the `evidence` object alongside the existing `xform` map and include it in the returned AppModel. Do NOT alter the plain-English synthesis (the two template-string branches stay byte-for-byte per D-13). Keep the field additive so the sample model (no evidence) still type-checks.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit && npx vitest run src/model --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `AppModel` has an `evidence` field; `adapt()` returns it populated from `m.evidence`; the xform synthesis template strings are unchanged (`git diff` shows no edit to them).
    - Behavior assertion: existing model parity tests (`npx vitest run src/model`) stay green; `tsc` clean.
  </acceptance_criteria>
  <done>AppModel carries an evidence map; adapt populates it; synthesis untouched; types compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inspector ColumnCard — Transform, Source→Target, Evidence, Connections, provenance line</name>
  <files>frontend/src/shell/Inspector.tsx</files>
  <read_first>
    - frontend/src/shell/Inspector.tsx (whole file — `resolveSelected`, `TableCard`'s `.sec`/`.sec-t` pattern, the already-destructured-but-unused `col`, the missing-field-omits-its-row guards, the existing single Esc-to-clear listener)
    - .planning/phases/03-lineage-dag-canvas-rebuild/03-UI-SPEC.md § "Inspector Column-Detail Layout" (section order + rationale, the ASCII layout, the locked caption) and § "Copywriting Contract" (Evidence caption, Provenance label)
    - frontend/src/styles/components.css (`.xform`, `.xform code`, `.xform p`, `.flow`, `.flow-item`, `.flow-item .dir`, `.sec`, `.sec-t` — reuse verbatim)
    - .planning/phases/03-lineage-dag-canvas-rebuild/03-PATTERNS.md § "frontend/src/shell/Inspector.tsx (extended, in-place)"
  </read_first>
  <action>
    Add a `ResolvedColumn` branch: when `useSelection().col` is set, resolve the owning table (from `sel`) and the selected column, then render a `ColumnCard`. ColumnCard renders, in order: (1) a provenance line under the title — a 10px inline SVG line with the inferred (dashed) `stroke-dasharray` and `stroke: var(--color-text-tertiary)` plus the single-word label 'Inferred' (achromatic, no colour meaning); (2) Transform `.sec` — `.xform code` = the transform expression from `model.xform[col][0]`, `.xform p` = the synthesized sentence `model.xform[col][1]`; omit the code block for a pass-through entry (detected via adapt's own stable copy — the 'Passed through' vs 'Computed as' sentence prefix, both owned by adapt.ts); (3) Source → Target `.sec` — one `.flow-item` per immediate source colEdge (↑) and the column itself (↓), each clickable calling `select()` with that column's ids; (4) Evidence `.sec` — from `model.evidence[col]`: a `text-micro` / `--color-text-secondary` header line naming notebook + cell index + line, the verbatim `snippet` in the same `.xform code` mono treatment, and the locked caption 'Inferred from static pattern-matching — not executed.'; omit the whole section when `model.evidence[col]` is absent; (5) Connections `.sec` — 'Upstream {N} · Downstream {N}' from a small directional count over `model.colEdges` for this column. Render every text value (snippet, names, expression) as a JSX text node so React escapes it — never through a raw-HTML injection prop. Reuse the existing Esc-to-clear listener unchanged (do not add a second one).
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit && npx vitest run src/shell/__tests__/Inspector.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: selecting a column with evidence renders a Transform code block, a Source→Target flow row, an Evidence snippet block, and the locked caption text 'not executed'.
    - Behavior assertion: selecting a pass-through column (null transform) renders the plain-English sentence with no `.xform code` block.
    - Source assertion (XSS gate): a comment-stripped scan of Inspector.tsx finds no raw-HTML injection prop — `grep -v "^\s*//" frontend/src/shell/Inspector.tsx | grep -c dangerouslySetInnerHTML` returns 0.
  </acceptance_criteria>
  <done>ColumnCard renders all five sections with correct omit rules; no raw-HTML injection; component test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Inspector.test.tsx — ColumnCard coverage (DAG-05 / TRUST-02)</name>
  <files>frontend/src/shell/__tests__/Inspector.test.tsx</files>
  <read_first>
    - frontend/src/shell/__tests__/Inspector.test.tsx (existing file if present — extend it; otherwise create it following the shell test idiom in src/routes/__tests__/rootPending.test.tsx for router/model context setup)
    - frontend/src/model/index.tsx (`sampleModel()` shape for building a fixture AppModel with evidence)
    - .planning/phases/03-lineage-dag-canvas-rebuild/03-VALIDATION.md § "Per-Task Verification Map" (DAG-05 / TRUST-02 row)
  </read_first>
  <behavior>
    - A column with evidence renders Transform code + plain-English sentence + Source→Target + Evidence snippet + the locked 'not executed' caption + a 'Upstream N · Downstream N' line.
    - A column whose model.evidence has no entry omits the Evidence section entirely (query returns null).
    - A pass-through column (xform sentence begins 'Passed through') renders no `.xform code` block, only the sentence.
  </behavior>
  <action>
    Add ColumnCard tests to Inspector.test.tsx: mount the Inspector with a fixture AppModel (via ModelProvider) and a selection where `sel` = table id and `col` = a column key, asserting the three behaviors above with Testing Library queries (`getByText`/`queryByText`). Provide `?sel`/`?col` through the same router/context harness the existing shell tests use. Keep one behavior per test, plain assertions.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/shell/__tests__/Inspector.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `npx vitest run src/shell/__tests__/Inspector.test.tsx` passes with the three new ColumnCard cases.
    - Behavior assertion: the missing-evidence case uses a `queryBy…` returning null (proves the section is omitted, not blank).
  </acceptance_criteria>
  <done>Three ColumnCard tests green covering render, missing-evidence omit, and pass-through code-block omit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parsed notebook text (evidence.snippet, notebook source) → Inspector DOM | Untrusted, regex-parsed code text is rendered to the user; this is the phase's primary XSS surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Tampering / EoP (reflected/stored XSS) | Inspector Evidence snippet render | high | mitigate | React's default JSX text-node rendering HTML-escapes all snippet/name/expression content; the plan forbids any raw-HTML injection prop in Inspector.tsx (verified by the comment-stripped grep gate in Task 2). No new code is required — the mitigation is the absence of a raw-HTML path. This is the canon phase-XSS threat from the security contribution (ASVS V5, block on high). |
| T-03-08 | Repudiation (false authority of inferred lineage) | Evidence caption copy | medium | mitigate | The locked caption 'Inferred from static pattern-matching — not executed.' is rendered under every Evidence block and must not be reworded (pitfall #6) — it prevents regex-derived evidence from reading as executed/semantic truth. |
</threat_model>

<verification>
- `cd frontend && npm run test:run` green.
- `cd frontend && npx tsc -b --noEmit` clean.
- Manual (phase-gate, non-blocking): select an inferred column in the running app; confirm the Evidence block shows the matched SELECT snippet + caption and that a `<script>`-containing fixture snippet renders as inert escaped text.
</verification>

<success_criteria>
- ColumnCard renders provenance line + Transform + Source→Target + Evidence + Connections (DAG-05/TRUST-02).
- Evidence omitted when absent; Transform code block omitted for pass-throughs.
- No raw-HTML injection path; locked caption present; synthesis unchanged (D-13).
</success_criteria>

<flagged_assumptions>
Spec-less probe rows resolved here:
- DAG-05 (`unclassified/unresolved`) → real edge is the missing-field-omits-its-row behaviour (null transform omits the code block; missing evidence omits the section), resolved from UI-SPEC Inspector partial-state and covered by Task 3.
- TRUST-02 (`unclassified/unresolved`) → real edge is per-cell evidence surfacing + the honest 'not executed' caption (pitfall #6); backend granularity/backward-compat is proven in plan 03-01, the render in Task 2/3. No numeric boundary exists.
</flagged_assumptions>

<output>
Create `.planning/phases/03-lineage-dag-canvas-rebuild/03-04-SUMMARY.md` when done.
</output>

---
phase: 03-lineage-dag-canvas-rebuild
plan: 07
type: execute
wave: 4
depends_on: ["03-01", "03-03", "03-05", "03-06"]
files_modified:
  - frontend/src/views/LineageDagView.tsx
  - frontend/src/views/LineageDagView.test.tsx
  - frontend/src/routes/__root.tsx
  - frontend/src/routes/lineage/$workspace.$lakehouse.$table.tsx
  - frontend/src/views/LineageView.tsx
autonomous: true
requirements: [DAG-01, DAG-03, DAG-04, DAG-06, DAG-08, TRUST-01, TRUST-03]
must_haves:
  truths:
    - "LineageDagView mounts a single <ReactFlow> with nodeTypes {tableNode, notebookNode} + edgeTypes {lineageEdge}, nodes/edges built from useDagreLayout + toXyflow for the current toggle mode, and imports the xyflow base layout reset (never the full themed stylesheet)"
    - "The <ReactFlow> instance sets nodesFocusable={false}, edgesFocusable={false}, disableKeyboardA11y, and nodesDraggable={false} so xyflow's default keyboard/drag model is fully replaced by the custom roving-tabindex hook (RESEARCH Pitfall 1; protects DAG-07 by preventing drag)"
    - "Hovering a column previews its trace transiently (dims unrelated to 0.15, non-interactive); a click freezes it as the persistent selection via useSelection().select(); hovering a different column while one is selected overrides the render transiently without mutating the URL, reverting to the selection when the hover ends (DAG-03/DAG-04, D-05/D-06)"
    - "Selection clears on Esc (existing shell listener), the Inspector close button, and an empty-canvas click (onPaneClick guard) — no second Esc handler is added (D-06)"
    - "A global Table/Column segmented toggle (default Column) in a new lineage-toolbar strip flips every card's mode simultaneously and calls useUpdateNodeInternals() for every table node so handles are re-measured and edges do not detach (DAG-06; RESEARCH Pitfall 2)"
    - "The lineage-toolbar strip also renders the FreshnessIndicator fed by AppModel.source + the root loader's fetchedAt (TRUST-03)"
    - "The canvas exposes a role=group wrapper with the accessible name 'Lineage graph: {N} tables, {N} notebooks, {N} connections', is one Tab stop, and carries an sr-only edge summary list ('{sourceCol} → {targetCol}, {kind}, {provenance} via {notebook}' per line) as the accessible text alternative for edges (DAG-08)"
    - "The lineage route renders LineageDagView (not the retired LineageView); LineageView.tsx is deleted and no import of it remains anywhere in src"
    - { statement: "E1 canvas empty — zero tables AND zero notebooks renders a centered 'No lineage to show yet' + body copy instead of an empty dot-grid xyflow instance", verification: backstop }
    - { statement: "E1 canvas overflow — content beyond the viewport is reachable via xyflow pan/zoom + fitView (no minimap); the canvas never clips lineage unreachably", verification: backstop }
  artifacts:
    - frontend/src/views/LineageDagView.tsx (+ LineageDagView.test.tsx)
    - frontend/src/routes/__root.tsx (RouterContext.fetchedAt)
    - frontend/src/routes/lineage/$workspace.$lakehouse.$table.tsx (LineageDagView swap)
    - (deleted) frontend/src/views/LineageView.tsx
  key_links:
    - "root loader fetchedAt -> RouterContext -> LineageDagView -> FreshnessIndicator (TRUST-03)"
    - "useDagreLayout + toXyflow (03-03) + TableNode/NotebookNode/LineageEdge (03-05) + useLineageKeyboardNav/FreshnessIndicator (03-06) -> composed here"
    - "toggle mode change -> useUpdateNodeInternals(id) per table node -> handles re-measured (DAG-06 correctness across toggle)"
  prohibitions:
    - "Do not import @xyflow/react/dist/style.css — import the base layout reset only (RESEARCH anti-pattern; keeps the token audit authoritative)"
    - "Do not extend xyflow's built-in node/edge selection (node.selected/onSelectionChange) as the persistence mechanism — persisted selection is the ?sel/?col store; hover is transient local state (RESEARCH anti-pattern, D-06/D-07)"
    - "Do not leave nodesDraggable enabled — drag would let a user break the deterministic dagre layout DAG-07 requires (RESEARCH Open Question #2)"
    - "Do not port LineageView.tsx's useLayoutEffect getBoundingClientRect()/anchor()/curve() measurement block — xyflow <Handle> + the custom edge replace it (Don't Hand-Roll)"
    - "Do not write selection through navigate({search}) anywhere but useSelection().select()/clear() (Phase-2 single-write-path contract)"
---

<objective>
Assemble the finished DAG canvas: `LineageDagView` mounts `<ReactFlow>` with the custom nodes/edges (03-05), consumes the pure layout/mapping core (03-03), wires hover-trace + persistent selection + the global Table/Column toggle + the roving-tabindex keyboard model + the sr-only edge alternative (03-06), and shows the freshness indicator (03-06) fed by a new root-loader `fetchedAt`. Finally it swaps the lineage route onto the new view and deletes the retired `LineageView.tsx`.

Purpose: This is the integration that turns the tested pieces into the working, keyboard-operable, provenance-aware column-lineage canvas — and the commit at which the old hand-rolled SVG view is retired without leaving the app half-migrated (SHELL-07 discipline).
Output: `LineageDagView` + smoke test, freshness-threaded root loader, swapped route, deleted legacy view.
</objective>

<artifacts_produced>
New symbols this plan creates (source-grounding exclusion list):
- `LineageDagView` (default-exported view component; props focusTable?/focusColumn?)
- `RouterContext.fetchedAt: number | null` (routes/__root.tsx)
- LineageDagView.test.tsx smoke cases (toolbar toggle, freshness, sr-only edge list, empty state)
- Removed: `LineageView` (default export of the deleted frontend/src/views/LineageView.tsx)
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-lineage-dag-canvas-rebuild/03-UI-SPEC.md
@.planning/phases/03-lineage-dag-canvas-rebuild/03-RESEARCH.md
@.planning/phases/03-lineage-dag-canvas-rebuild/03-PATTERNS.md
@frontend/src/views/LineageView.tsx
@frontend/src/routes/__root.tsx
@frontend/src/routes/lineage/$workspace.$lakehouse.$table.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread fetchedAt through the root loader (TRUST-03 source)</name>
  <files>frontend/src/routes/__root.tsx</files>
  <read_first>
    - frontend/src/routes/__root.tsx (the loader returning `{ graph }`, RouterContext interface, RootComponent, RootPending)
    - .planning/phases/03-lineage-dag-canvas-rebuild/03-UI-SPEC.md § "Freshness indicator › Source, no new persistence (D-14)"
    - frontend/src/model/index.tsx (`AppModel.source` live/sample — the fallback signal)
  </read_first>
  <action>
    In routes/__root.tsx change the loader to capture the fetch time: return `{ graph, fetchedAt: graph ? Date.now() : null }` (only set when a real graph resolved; null on the silent sample fallback). Add `fetchedAt: number | null` to `RouterContext`. This is an in-memory, session-only value (no persistence, D-14). Do not change the pendingComponent/overlays behaviour or the adapt/sampleModel selection.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit && npx vitest run src/routes/__tests__/rootPending.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the loader returns a `fetchedAt` alongside `graph`; `RouterContext` declares `fetchedAt: number | null`.
    - Behavior assertion: existing rootPending regression test stays green; `tsc` clean.
  </acceptance_criteria>
  <done>Root loader captures an in-memory fetchedAt exposed on RouterContext; no persistence, no pending-state regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LineageDagView — compose the canvas (trace, selection, toggle, keyboard, freshness, a11y)</name>
  <files>frontend/src/views/LineageDagView.tsx, frontend/src/views/LineageDagView.test.tsx</files>
  <read_first>
    - frontend/src/views/LineageView.tsx (the trace/selection/empty-canvas-click patterns to carry forward; the useLayoutEffect measurement block to NOT port)
    - frontend/src/views/lineage-dag/ (all of 03-03's useDagreLayout/toXyflow/trace/types and 03-05's TableNode/NotebookNode/LineageEdge/lineage-dag.css and 03-06's useLineageKeyboardNav/FreshnessIndicator)
    - frontend/src/selection/useSelection.ts (the select/clear single write path)
    - .planning/phases/03-lineage-dag-canvas-rebuild/03-UI-SPEC.md § "Layout & Composition" (lineage-toolbar strip, Background dots, no minimap), § "Trace & Selection Interaction Contract", § "Keyboard & AT Traversal Model" (role=group wrapper, sr-only edge list, accessible names), § "Freshness Indicator & Table↔Column Toggle", § "Copywriting Contract" (empty-state copy)
    - .planning/phases/03-lineage-dag-canvas-rebuild/03-RESEARCH.md § "Common Pitfalls › Pitfall 1 & 2" (disable default keyboard, useUpdateNodeInternals on toggle) and § "Architecture Patterns › System Architecture Diagram"
  </read_first>
  <behavior>
    - Renders a lineage-toolbar with a Table/Column segmented control defaulting to Column, and a FreshnessIndicator.
    - With a fixture AppModel that has tables/notebooks, renders a role=group wrapper whose accessible name reports the table/notebook/connection counts, and an sr-only list with one entry per edge.
    - With an empty AppModel (0 tables, 0 notebooks), renders the 'No lineage to show yet' copy and does not mount the xyflow canvas.
    - Toggling to Table calls useUpdateNodeInternals for the table nodes (mode change re-measures handles).
  </behavior>
  <action>
    Create LineageDagView.tsx (default export, props `{ focusTable?, focusColumn? }`) and its lineage-dag.css + xyflow base reset imports. Read the model via useModel() and fetchedAt via RootRoute.useLoaderData(). Hold local state: `mode: 'table' | 'column'` (default 'column'), `hover: string | null`, and read persisted selection via useSelection(). Compute `active = hover ?? selectedCol`; `traced = active ? trace(model.colEdges, active) : null`. Memoize dagre positions via buildDagreLayout(model.tables, model.notebooks, model.ops, mode) and the xyflow nodes/edges via toXyflow(...), decorating each node/edge with trace-derived class/opacity state and mode. Mount `<ReactFlow nodes edges nodeTypes={{tableNode: TableNode, notebookNode: NotebookNode}} edgeTypes={{lineageEdge: LineageEdge}} nodesFocusable={false} edgesFocusable={false} disableKeyboardA11y nodesDraggable={false} onPaneClick={clear-on-empty} fitView>` with `<Background variant="dots" gap={22} size={1.2} color="var(--color-grid-dot)" />` and no minimap. Wire the toolbar seg toggle → setMode + call useUpdateNodeInternals(id) for every table node id in an effect keyed on mode. Wrap the canvas in a `role="group"` element with the accessible-name template and attach the useLineageKeyboardNav onKeyDown; render a visually-hidden (`sr-only`, kept in the a11y tree) edge summary list. Column hover sets local hover; column/ header click calls select() (single write path); empty-pane click calls clear(). Render the empty-state copy when tables.length===0 && notebooks.length===0. Import the xyflow base layout reset once here (base reset only — not the themed stylesheet). Add LineageDagView.test.tsx covering the four behaviors above (mount within ReactFlowProvider + ModelProvider + a router/loader harness).
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit && npx vitest run src/views/LineageDagView.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `npx vitest run src/views/LineageDagView.test.tsx` passes the toolbar-toggle, freshness, sr-only-edge-list, and empty-state cases.
    - Source assertion: `<ReactFlow>` sets `nodesFocusable={false}`, `edgesFocusable={false}`, `disableKeyboardA11y`, `nodesDraggable={false}` (grep each present) — xyflow's default keyboard/drag model is replaced, not extended.
    - Source assertion: the file imports the xyflow base reset and grep finds no `dist/style.css` import; no `useLayoutEffect`+`getBoundingClientRect` measurement block was ported.
    - Behavior assertion: the empty AppModel case renders 'No lineage to show yet' and mounts no xyflow canvas.
  </acceptance_criteria>
  <done>LineageDagView composes layout + nodes/edges + trace/selection + toggle (with handle re-measure) + keyboard + freshness + sr-only edge list + empty state; default keyboard/drag disabled; smoke test green.</done>
</task>

<task type="auto">
  <name>Task 3: Swap the lineage route onto LineageDagView and delete LineageView.tsx</name>
  <files>frontend/src/routes/lineage/$workspace.$lakehouse.$table.tsx, frontend/src/views/LineageView.tsx (deleted)</files>
  <read_first>
    - frontend/src/routes/lineage/$workspace.$lakehouse.$table.tsx (renders `<LineageView focusTable focusColumn>`; swap the import + element)
    - frontend/src/views/LineageView.tsx (the file to delete — confirm nothing else imports it)
  </read_first>
  <action>
    In routes/lineage/$workspace.$lakehouse.$table.tsx replace the `LineageView` import with `LineageDagView` and render `<LineageDagView focusTable={focusTable} focusColumn={search.col} />` (identical prop contract). Delete frontend/src/views/LineageView.tsx. Confirm no remaining import of `LineageView` exists anywhere in src (grep). Leave the route's defensive focusTable resolution logic unchanged.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit && npm run test:run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `grep -RIn "LineageView" frontend/src` returns no matches (the legacy view is fully retired, no stale import).
    - CLI output: `npm run build` (tsc -b && vite build) succeeds; `npm run test:run` full suite green.
    - Behavior assertion: navigating the lineage route renders LineageDagView (the route element references it).
  </acceptance_criteria>
  <done>Lineage route renders LineageDagView; LineageView.tsx deleted; no stale imports; full build + suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed data (names, notebook labels) → canvas + sr-only edge list | Untrusted parsed text is rendered into node labels and the accessible edge summary |
| keyboard/pointer input → focus + selection + toggle | User input drives view state; persisted selection is URL state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-14 | Tampering / EoP (XSS via node/edge-summary text) | LineageDagView labels + sr-only edge list | high | mitigate | All labels/summaries render as JSX text nodes (React auto-escapes); no raw-HTML injection prop anywhere in the view. Base xyflow reset only (no themed sheet). Canon phase-XSS mitigation (ASVS V5). |
| T-03-15 | Tampering (non-deterministic layout via drag) | ReactFlow nodesDraggable | medium | mitigate | `nodesDraggable={false}` prevents a user from dragging cards and breaking the deterministic dagre layout DAG-07 requires. |
| T-03-16 | Denial of Service (a11y focus trap / stale handles) | keyboard nav + toggle | medium | mitigate | Canvas is one Tab stop with no focus trap; `useUpdateNodeInternals` on toggle keeps edges attached so keyboard + visual navigation stay coherent. |
</threat_model>

<verification>
- `cd frontend && npm run test:run` full suite green; `npm run build` succeeds.
- `cd frontend && npm run audit:tokens` exits 0.
- Manual (phase-gate, non-blocking, standing pitfall #12/#19 discipline before /gsd-verify-work):
  - Light-mode visual review of the rebuilt canvas (edges, cards, dim-others, inspector) — no raw-hex leaks, contrast holds.
  - Provenance solid-vs-dashed distinguishable under deuteranopia/protanopia independent of edge-type hue.
  - Full keyboard/AT walk: Tab into the canvas, walk cards/columns and path-walk edges by keyboard alone; verify focus-visible rings + AT announcements of node/edge identity and provenance; confirm the sr-only edge list is present in the a11y tree.
</verification>

<success_criteria>
- LineageDagView renders the column-lineage DAG with hover-trace, persistent selection, the global toggle (with handle re-measure), keyboard operability, freshness, and the sr-only edge alternative.
- xyflow default keyboard/drag disabled; base reset only; no themed-sheet import.
- Lineage route swapped to LineageDagView; LineageView.tsx deleted; full build + suite green.
</success_criteria>

<flagged_assumptions>
Spec-less probe rows resolved here (integration):
- DAG-03/DAG-04 (`unclassified`) → real edges are transient-hover-preview vs click-freeze persistence, and selection surviving a subsequent hover without URL mutation; resolved from D-05/D-06 + UI-SPEC Trace & Selection, wired in Task 2 (algorithm proven in 03-03's trace test).
- DAG-06 (`boundary`/`precision` — flagged FALSE POSITIVE for a view toggle) → confirmed no numeric contract; the real edge is handle re-measurement across the toggle (useUpdateNodeInternals) so edges do not detach, wired in Task 2.
- DAG-08 (`unclassified`) → real edge is the single-Tab-stop role=group wrapper + sr-only edge alternative + replaced (not extended) keyboard model; wired in Task 2 (resolver proven in 03-06).
- TRUST-03 (`unclassified`) → real edge is the live-time vs bundled-sample honesty fed by the new root-loader fetchedAt; wired in Task 1/2 (component proven in 03-06).
</flagged_assumptions>

<output>
Create `.planning/phases/03-lineage-dag-canvas-rebuild/03-07-SUMMARY.md` when done.
</output>
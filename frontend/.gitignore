# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# TanStack Router plugin cache
.tanstack/

# NOTE: src/routeTree.gen.ts is generated but IS committed. `npm run build` is
# `tsc -b && vite build`, so tsc typechecks before the vite plugin regenerates
# the tree — on a fresh CI clone an ignored file makes the build fail with
# "Cannot find module './routeTree.gen'". Committing it keeps a clean checkout
# typecheckable. The plugin still regenerates it on every dev/build run; commit
# the result whenever routes change.

# Added by `vercel link`. `.env.local` holds a downloaded OIDC token, and
# `.vercel/` the project id — neither belongs in the repo.
.vercel
.env*
# ...but the template does: it is the only record of which VITE_* vars a build
# expects, and a blanket .env* would hide it from anyone adding one.
!.env.example

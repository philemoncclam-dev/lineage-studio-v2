// Non-modal overlay Inspector STUB (D-10/D-12/D-13). AppShell mounts this
// unconditionally as the fixed overlay mount point 02-05 fills in with the
// real metadata card driven by useSelection(). Renders null until then —
// intentionally inert, not a placeholder page (nothing is user-visible
// here yet, so there is nothing to render honestly-empty).
export default function Inspector() {
  return null
}
